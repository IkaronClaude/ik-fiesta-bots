# Measurements harvested from code comments (comment-scrub P0, 2026-08-18)

Moved out of the source per the operator: provenance drifts like any other comment.
The code keeps at most a one-line pointer where a number justifies a constant.

- **src/Fiesta.Bot.Host/BotEndpoints.cs:119** — ⚠️⚠️ `max` IS A REQUEST SIZE, NOT THE BUFFER SIZE. The ring buffer holds 100_000 lines (BotHandle.MaxLogLines) — roughly 5 hours of verbose history at the measured ~6 lines/sec. If you do not pass `max` you get the default and silently throw away almost all of it. This cost real accuracy: with the old 200 default, a level=verbose read returned NINE SECONDS, and "0 kills, 0 QUEST_MOB_KILL — the bot completes nothing" was reported off exactly that sample while the bot was in a town phase and actually at q52 5/8 and climbing. GET /log?level=note&max=100000 <- headlines, hours of history GET /log?level=info&from=13:41:15&to=13:42:00 <- drill into one moment A NARROW SAMPLE IS NOT A NEGATIVE RESULT: "I did not see X" means nothing until you know the window was wide enough to have contained X. ── METRICS / BOT-WATCH (operator epic 2026-08-05: "a window into everything going on with the bot; like a stat panel, you look at it and immediately know where the bot is") ──────────────────────── Lightweight companion to /metrics, for the watch page's COMBAT MAP. That map wants ~5 updates a second and mob positions change constantly, but /metrics carries the whole metric snapshot, the quest board and the trace counts — far too heavy to poll at that rate. This returns only what the map draws.
- **src/Fiesta.Bot.Host/BotEndpoints.cs:1235** — SOUL STONES AS COOLDOWN TILES (operator 2026-08-13: "display current stone cooldown as a skill-icon-sized button basically exactly like skills"). Same three fields a skill tile renders from, so the page can reuse the tile renderer rather than growing a second one. ⭐ SP CARRIES A REAL COOLDOWN NOW (operator 2026-08-13: "both hp and sp cooldowns are 7 seconds"). It had been published as null — "unknown" — because only the HP gap had been measured and I would not assume the two matched; the operator supplied the fact. SP reads the SAME value the HP side uses rather than a second constant, and its remaining clock comes from the SP stone's own USESUC (0x500A), which was being handled but not timestamped.
- **src/Fiesta.Bot.Host/EventStreamEndpoint.cs:82** — <summary>THE FULL-STATE PACKET — everything the viewer needs, from ONE read, at ONE instant. <para>⛔ THE CLIENT MUST NEVER HAVE TO RECONCILE TWO SOURCES (operator 2026-08-13: <i>"our stream endpoint should start by sending a first 'full state' packet (similar to a map change packet) rather than us relying on 2 separate data sources which can not match (e.g. if stream is called 20ms later we could miss a mount packet or whatever)"</i>). A page that stitches a polled snapshot together with a live delta feed has no way to know which is newer: the two are read at different instants, and anything that happened in the gap — a mount, a death, a map change — is either double-counted or lost, with no symptom except a picture that is quietly wrong.</para> <para>So the stream is SELF-SUFFICIENT: it opens with this, and re-sends it whenever the world is wholesale replaced (a map change), exactly as the game's own login burst does. The poll survives only for a host with no WebSocket at all, and is never mixed in.</para> <para>Ordering matters and is not accidental: the pump subscribes to the change events BEFORE building this snapshot. Subscribing after would open a window where a change lands between the read and the subscription and is lost forever; this way the worst case is an update that repeats one already in the snapshot, and every message here is an idempotent upsert.</para></summary>
- **src/Fiesta.Bot.Host/Program.cs:209** — The minimap's WORLD EXTENT, so the page can place a bot on the art instead of guessing a scale. MEASURED, not assumed (tools/minimap_orient.py): the image spans the full square .shbd grid — world [0, tiles*6.25] on both axes — with the Y axis FLIPPED. Correlating each map's walkability mask against its painted ground scored flipY above every other orientation on every map that discriminates (EldGbl02 0.880 vs 0.415 identity, RouVal02 0.635 vs 0.395, Urg 0.494 vs 0.286) and never lost; towns like RouN are inconclusive because they are mostly rooftops. This matches the operator's earlier read of the .shbd ASCII render ("y is flipped but that render is pretty accurate").
- **src/Fiesta.Bot.Host/Program.cs:338** — WAIT FOR THE REAL SIGNAL, NOT A GUESS. This was `await Task.Delay(2 minutes)` — a fixed timer standing in for "has the startup restore finished", and it cost exactly what a fixed timer always costs. Measured 2026-08-12 after a deploy: the pod came up at 17:19, three bots lost their zone link ~6s into the fresh login (the server had not yet released the pre-restart session), and then sat `Failed` — earning nothing, logged in nowhere — until 17:21:20, when the timer finally expired and the first pass repaired all three within seconds. Two minutes of guaranteed downtime after EVERY deploy, for a restore that takes 3 seconds per bot. Awaiting the restore's own task makes the wait exactly as long as the work it is waiting for.
- **src/Fiesta.Bot.Host/ScriptEndpoints.cs:230** — <summary>Body for <c>POST /api/bots/{id}/script</c>. Give a stored <c>Name</c> OR inline <c>Source</c> (optionally labelled via <c>NameAs</c>). <c>TickMs</c> overrides the loop interval (default 250 ms).</summary>
- **src/Fiesta.Bot/ClientData/ClientData.cs:279** — CastTime = the CAST ANIMATION length (ActiveSkill.shn col 26), distinct from DlyTime (the cooldown). While it runs the character is locked in the cast, so firing another skill during it is wasted — that is how castRotation ended up sending FIVE casts in 18ms, each one's STOP cancelling the melee swing stream (see the CEASE_FIRE ticket / packets-JcqFresh.log).
- **src/Fiesta.Bot/ClientData/ClientData.cs:302** — STUN: does any abnormal-state effect (StaNameA..D) apply a STUN? A stun skill (Concussive Charge = StaBattleBlowStun, 100%) freezes the target so the bot can safely kite+heal on low HP (operator "stun and kite on low hp"). Detected by the "Stun" substring in the state name — data-driven, NO hardcoded skill id. MaxWc is 0 for these (utility), so they were never in the damage rotation.
- **src/Fiesta.Bot/ClientData/ClientData.cs:475** — What the vendor CHARGES. Without it the driver could not tell "I want this" from "I can afford this", so it fired buy after buy the server rejected with 0x020B for lack of money — measured 2026-08-11: JcqFighter had 131 cen and kept buying a 290-cen mastery book, JcqCleric had 95 and kept buying a 380-cen necklace, both looping in town instead of grinding for the money that would have made the purchase possible.
- **src/Fiesta.Bot/ClientData/ClientData.cs:633** — A stun ALSO carries the action-block action (25) alongside immobilize (19); a root/entangle carries 19 alone. Derived from the client SHNs exactly like the immobilize rule — the InxNames only VALIDATE it, they are never matched on. Validated over all 107 immobilizing SubAbState rows (2026-08-06): 19 + 25 (n=82) -> 46 "…Stun" names (SubStaBattleBlowStun, SubStaShockBladeStun, …) 19 alone (n=25) -> 16 "…Entangle" + 7 "…Bind" (SubStaSpiritThornEntangle, SubStaMarloneEntangle, …)
- **src/Fiesta.Bot/ClientData/ClientData.cs:937** — <summary>Does the client's OWN matrix allow class <paramref name="classId"/> (a ClassName <c>ClassID</c>, 1-27) to use an item/scroll whose ItemInfo <c>UseClass</c> is <paramref name="useClass"/>? THIS is the real answer; <see cref="UseClassLineFor"/> is a hand-written approximation of it and is wrong in four ways (kept only as a fallback). <para><b>The table:</b> <c>UseClassTypeInfo.shn</c> — 39 rows, one per UseClass value, each with a 0/1 flag column per class. The 27 flag columns are the 27 ClassName ClassIDs IN ORDER (col 1 = Fig = ClassID 1 … col 27 = Sav = ClassID 27), so the lookup is by ORDINAL, never by name: the abbreviations do not match ClassName's acPrefix (Cfig/Cfi, Hcle/Hcl, Harc/Har, Wmag/Wma) and acPrefix "War" is AMBIGUOUS — it is both Warrior (3) and Warlock (19).</para> <para><b>What the ladder got wrong</b> (measured against the table + ItemInfo, 2026-08-11):</para> <para>• <b>UseClass 1 = EVERY class</b> (row 1 has all 27 flags set) and it is 7465 of the 14999 items — half the item file. The ladder does not contain 1 for anyone, so it rejected all-class gear and consumables outright. <c>bestRewardIndex</c> had already worked this out independently (<c>uc == 1 || line.Contains(uc)</c>) — the two disagreed.</para> <para>• <b>UseClass 0 is used by NOTHING</b> — zero items carry it, and its row has every flag CLEAR. The old <c>canUseClass</c> special-cased 0 as "everyone", which is exactly backwards; the all-class value is 1. (0 being meaningful-but-empty here is not a licence to treat 0 as "unset" elsewhere — see the golden rule in CLAUDE.md.)</para> <para>• <b>The ladder was too PERMISSIVE for a base class.</b> It gave a Fighter (ClassID 1) the whole band 2-7, but the table says Fig is only in UseClass 2 — 3-7 are the ADVANCED classes (Cfig/War/Gla/Kni). So a fresh Fighter believed it could use gear and scrolls that require a job change, i.e. it bought unusable items even on the one class we thought worked.</para> <para>• <b>Cross-line and near-universal values were missing entirely:</b> 26 = everyone except Sen/Sav; 35-38 are cross-line combos (35 = War/Gla/Kni + Sco/Sha/Ran). The ladder rejects all of them for every class.</para> <para>Verified against a known item: "Slice and Dice [01]" (id 4700), the Fighter scroll a Mage bought live, is <c>UseClass 2</c> → Fig/Cfig/War/Gla/Kni, which correctly excludes Mage (16).</para> </summary> <summary>If <paramref name="productId"/> is a CRAFTING RECIPE, the job it needs and how many job points in that job — else null (an ordinary skill book / item). <para><c>Produce.shn</c> is keyed by <c>ProductID</c>, which is the same id as the recipe skill and its book, so a plain lookup answers "is this a recipe, and can we ever learn it?". Verified against the two books that were live-looping on 2026-08-11: <c>24074 Recipe_R_LowToadStool → NeededMasteryType 5, NeededMasteryGain 100</c> and <c>23073 Recipe_C_NorToadStool → type 4, 100</c> — matching the operator's "shows min skill points: 100". <c>ProduceView.shn</c> names the five mastery types (1 = Potion Production, …).</para> <para>⛔ I PREVIOUSLY CONCLUDED THIS WAS NOT IN CLIENT DATA and nearly shipped a comment saying so. It was — I had searched column names for "job" and file names for "craft/recipe", and this table calls the concept MASTERY and the file is called Produce. The operator was right; the absence of a match for MY chosen keyword was not evidence of absence. Recorded because "I searched and it isn't there" is exactly the claim that needs the most scepticism.</para></summary>
- **src/Fiesta.Bot/ClientData/MinimapImage.cs:3** — <summary> Loads a map's MINIMAP — the same picture the real client draws — from BYO client data at <c>&lt;client root&gt;/resmenu/minimap/&lt;MapName&gt;.{tga,dds}</c>, and hands it back as a PNG. <para><b>Client-data traps, all settled by measurement:</b></para> <list type="number"> <item><b>Mixed case.</b> The extensions are spelled <c>.dds</c> and <c>.DDS</c> in one directory, so the lookup is case-insensitive — the same trap that silently cost the item icons their art twice.</item> <item><b>⛔ The <c>.tga</c> files are NOT minimaps — they are LOADING SCREENS.</b> Only two exist (LinkField01/02) and both are 1024x768 (a 4:3 screen, not a square map); decoding LinkField02.TGA renders the "Dark Passage II — Now Loading" splash art. An earlier version of this loader preferred TGA over DDS on the reasoning that it was higher-resolution; that was wrong, and would have drawn a picture of four chibi adventurers where the map belongs. <b>DDS only.</b></item> <item><b>Those TGAs also look blank in a viewer</b> — 16bpp ARGB1555 with the alpha bit clear on every pixel, so anything honouring alpha shows nothing. The TGA decoder below forces opaque and is kept for <c>tools/tga_to_png.py</c>-style inspection, but it is not used for minimaps.</item> </list> <para><b>World↔image mapping (MEASURED — tools/minimap_orient.py).</b> The image spans the FULL square <c>.shbd</c> grid, world <c>[0, tiles*6.25]</c> on both axes, with <b>Y FLIPPED</b>: <c>px = worldX/extent*W</c>, <c>py = (1 - worldY/extent)*H</c>. Established by correlating each map's walkability mask against its painted ground across all four flip candidates: flipY scored highest on every map that discriminates (EldGbl02 0.880 vs 0.415 identity, RouVal02 0.635 vs 0.395, Urg 0.494 vs 0.286) and never lost. Towns (RouN, Eld) come out inconclusive because they are mostly rooftops, so they neither confirm nor contradict. An earlier attempt to fit by drawn-pixel bounding box was a dead test — minimaps are fully painted, so every image's content bbox is the whole image.</para> </summary>
- **src/Fiesta.Bot/Manager/BotHandle.cs:30** — Big enough that a verbose firehose does not evict history before anyone reads it. 1500 was FAR too small: measured 2026-08-05, at level=verbose 1500 lines spanned 63 SECONDS and the endpoint's 200-line default spanned NINE. That produced real misdiagnoses — a "0 kills, the bot completes nothing" report that was a 9-second sample taken during a town phase, and a bag census evicted before it could be read. A narrow sample is not a negative result, so the buffer must be wide enough that "I did not see X" actually means something. 100k lines ~= 10MB, which is nothing next to what this process already holds (operator 2026-08-05: "we'll survive having 10MB worth of buffer"). Still filtered per-level on the way out, so a level=note read stays cheap.
- **src/Fiesta.Bot/Manager/BotHandle.cs:40** — ⛔ AND A CHARACTER BUDGET, because the line cap alone does not bound MEMORY. The note above estimated "100k lines ~= 10MB" from ~100-byte lines — but MEASURED at verbose, a combat line averages 253 bytes, so the ring actually held ~53MB per bot and ~212MB across four. That, not any leak, is what put the pod at 503Mi against its 512Mi limit and produced 51 OutOfMemoryExceptions surfacing as "Dictionary.Resize()" inside whatever allocated next — a symptom that reads like a runaway collection and is nothing of the sort. Budgeting CHARS instead of lines keeps the full 100k when they are short (a level=note history still spans hours) and trims automatically when verbose combat spam makes them long.
- **src/Fiesta.Bot/Manager/BotHandle.cs:49** — Trim in CHUNKS, never one line at a time: List.RemoveRange(0, 1) memmoves the entire backing array, so trimming per line at a full buffer copied 100k entries for EVERY line logged — 4 bots x ~6 lines/sec x 100k moves. That was burning most of a core (measured 928m against a 1000m limit).
- **src/Fiesta.Bot/Manager/BotHandle.cs:73** — <summary>Whether the LAST quest-dialogue drive (DriveQuestDialogueAsync) reached its terminal page (Qsc 0x06 ACCEPT / 0x0A DONE) for OUR quest — i.e. the accept/hand-in actually CONCLUDED on the wire. The leveler reads this after a hand-in drive to know the hand-in succeeded even when a REPEATABLE quest immediately re-accepted (so bot.questProgress reads a stale 10/10 and questReadyToHandin loops).</summary> <summary>When we last sent BASHSTART. A cast issued before the swing windup elapses (median 418ms, measured) trades the whole swing for the cast — see docs/COMBAT_BIBLE.md.</summary>
- **src/Fiesta.Bot/Manager/BotHandle.cs:147** — ── AUTO-RELOG PACING ──────────────────────────────────────────────────────────────────────── A zone drop used to trigger an INSTANT relog, which races the server's own session cleanup: the new login completes, the server still holds the old zone session, and it closes the new one immediately (`uptime 0s, lastSENT=0x0000`). That relogs again, and so on. Measured 2026-08-12 on JcqFresh: 23 of 26 relog gaps were under 10s (2.0-7.3s), turning ONE real disconnect into a ~2-minute storm of ~20 logins — each re-seeding 33 quests, 27 skills, 36 items and restarting the driver's town pass. JcqArcher: 243 peer-closes, 100 relogs, 155 sessions dead inside 3 seconds. The bots always recovered; the cost was the thrash. This is not a retry-cap crutch: waiting for the server to release the previous session is the correct SEQUENCING for a protocol that binds one session per character. <summary>When this bot last completed zone entry. A session that dies seconds after this is the server refusing a login whose predecessor is still registered — see the relog pacing note.</summary>
- **src/Fiesta.Bot/Manager/BotHandle.cs:384** — ── LIVE MOTION SEGMENT (operator 2026-08-13) ──────────────────────────────────────────────── "if we walk from A to B and we're 0.5 sec into it, and change path to C, do we send a packet from 0.5*(A+B) towards C, or from B? Because that WOULD movefail (suddenly doing a step that's 1.5 steps far => fucked)" Exactly right, and both of the obvious implementations are wrong: • committing B at SEND time -> every re-path starts from a place we never reached (measured: "believed @(2015,3528), server snapped to (2114,3526), delta=99"; 44% of moves rejected). • committing B only AFTER the travel delay -> a mid-step re-path starts from A instead, which is the same error mirrored (up to a full 250u step behind). So position is not a stored POINT, it is a stored SEGMENT evaluated against the clock: the bot is at from + (to-from) * clamp(elapsed/duration, 0, 1), which IS 0.5*(A+B) half a second in. A real client reports its true current position on every frame — measured `from`==previous `to` on only 2% of frames (Z:/LongCaptureNoDc.pcapng, n=20337) vs our 48-90% before this.
- **src/Fiesta.Bot/Manager/BotHandle.cs:558** — <summary> Turn to face (toX,toY) from (fromX,fromY) WITHOUT claiming to have arrived there. Facing is instant; travel is not. The streaming walker sends a MOVERUN, turns immediately, and only commits the new position once the travel time has actually elapsed — committing the destination at send time is what put our tracked position up to 121u ahead of the server's and got the next leg rejected (44% of our moves vs a real client's 0.25-0.6%). </summary>
- **src/Fiesta.Bot/Manager/BotHandle.cs:798** — The polled snapshot stays readable: headline + progress only (drop the verbose firehose). Pull the full stream (incl. Verbose) from the /log tail endpoint when chasing a bug. ⛔ THIS WAS THE RESTART LOOP (measured 2026-08-18). The comment said "headline + progress only", but the code had NO LIMIT: it scanned all 100k ring entries and allocated an array of EVERY Note+Info line, on EVERY Snapshot() -- i.e. every GET /api/bots (x4 bots) and every GET /api/bots/{id} -- while holding _logGate, the lock all four bots need to WRITE a log line. Measured consequence: GET /api/bots took 23.8s to list four bots, cgroup at 500.2MiB/512MiB (97.7%) and CPU throttled in 192 of 519 periods (30.8s throttled per 52s wall), so /health could not answer inside the 5s liveness timeout -> kubelet SIGKILLed the container (exit 137, 6 restarts). Every restart drops all four bots. Same shape as the log-ring OOM fixed in 46c6b8a: a comment asserting a bound the code never implemented. Walk BACKWARDS and stop at SnapshotLogLines. No full scan, no unbounded allocation, and the lock is held for ~25 iterations instead of 100k.
- **src/Fiesta.Bot/Manager/BotLogFile.cs:105** — ⛔ NEVER File.ReadAllLines HERE (fixed 2026-08-18). These rotated files are 1-4.6MB each (100MB total on disk), and reading one whole just to keep its TAIL allocated a string[] of every line — for up to 10 files x 4 bots, all during spawn. That transient spike is what OOMKilled the container 96s after start (exit 137, reason=OOMKilled) and dropped all four bots. Stream instead, keeping only the last `take` lines in a fixed ring.
- **src/Fiesta.Bot/Manager/BotManager.cs:83** — ⛔ A DEAD BOT MUST NOT HOLD ITS ID HOSTAGE (operator 2026-08-18: "fix the problem of 'failed' respawns"). A Failed/Stopped bot is a corpse: it cannot self-heal (the liveness watchdog only runs while Phase==InZone), so the id stayed occupied and EVERY respawn — the supervisor's and any manual one — got a 409. The supervisor worked around it by calling StopAsync(forget:false) then sleeping 3s, which is both racy and invisible to an API caller; measured live this session, all four bots sat Failed while POST /api/bots returned 409. A corpse carries no session and no script runner, so replacing it is safe and idempotent. Only a LIVE bot is a genuine conflict.
- **src/Fiesta.Bot/Manager/BotManager.cs:217** — ⛔ NO ZONE LINK IS NOT THE SAME AS NOTHING TO LOG OUT. A Failed bot has a dead zone session but its WM link is usually STILL OPEN — measured 2026-08-11 on three Failed bots at once: WM uptimes 495s / 613s / 435s. The WM link is what holds the ACCOUNT online (there is no character-level ghost — operator 2026-08-11), so cancelling without logging it out leaves a STALE SESSION, and the next login on that account on that ACCOUNT is duplicate-kicked: login and WM both succeed, CHAR_LOGIN_ACK arrives, and then the zone drops the connection ~76ms after MAP_LOGIN_REQ. That is precisely the failure this method's own comment above predicts ("leaves the char 'online' -> the next login is duplicate-kicked") — the clean-logout branch just never covered the case where only the ZONE had died. Operator: "You should be able to log 5 accounts in at the same time no problem — as long as they don't have stale sessions." Concurrency was never the issue; unclean stops were, and this is where they were manufactured.
- **src/Fiesta.Bot/Manager/BotManager.cs:275** — ⛔ forget:false — A RELOG IS NOT AN OPERATOR STOP. StopAsync DE-PERSISTS the bot from the restore roster by default, and Relog used that default: it removed the bot, then re-Spawn re-added it. Anything landing in the gap loses the bot PERMANENTLY — neither the startup restore nor the supervisor can bring back an id that is no longer in the roster, because both iterate over exactly that roster. The gap is not small: 3s settle, then up to 90s waiting for zone entry, times up to four attempts, and every deploy rolls the pod straight through it. Worse, the give-up path below leaves the bot Failed AND forgotten, with a comment promising "next cron will respawn" — there is no cron, and the supervisor only knows persisted bots. That is the "a bot vanished from the roster ENTIRELY" P0: measured 2026-08-12, the host came back up reporting "restoring 2 bot(s)" when five were meant to be running.
- **src/Fiesta.Bot/Manager/BotManager.cs:567** — ⛔ NEVER FACE-STEP WHILE A BASH IS RUNNING. Every MOVERUN in this codebase comes from FaceAndStopAsync, and a MOVERUN mid-swing cancels the swing that was about to land — measured post-fix: SWING_START at +272ms, our face-step at +276ms, CEASE_FIRE at +378ms, no damage. The BASH ITSELF already faced the target (AutoAttackAsync face+stops before BASHSTART), so the step tells the server nothing it does not know and costs the whole swing. Downgrade to a bare STOP, which is the part that is actually load-bearing for a cast. This is also the remaining source of 0x0FCA "precondition unmet, suspect MOVING".
- **src/Fiesta.Bot/Manager/BotManager.cs:627** — ── FIX 1 of 3: DO NOT CAST INTO A BASH THAT HAS NOT SWUNG YET ──────────────────────────── First damage lands a MEDIAN 418ms after BASHSTART (the swing windup). We were casting ~4ms after it, every time, so the swing never happened: 81% of our bashes produced ZERO damage against the real player's 4%. The player casts AFTER SWING_START is acknowledged. Defer the cast (the caller retries next tick) rather than trading a whole swing for it.
- **src/Fiesta.Bot/Manager/BotManager.cs:674** — Open the cast-animation window SPECULATIVELY, right on send. Waiting for the server's CAST_SUC_ACK would leave a round-trip hole in which the driver fires more casts (that is precisely how 5 casts went out in 18ms). ZoneView then confirms/ends this from the server's own packets — the local ActiveSkill.CastTime is only the un-wedge deadline.
- **src/Fiesta.Bot/Manager/BotManager.cs:680** — 📐 LOG THE GEOMETRY WITH THE CAST. 0x0FC0 is the bot's #1 combat failure (1281 in one session, every skill, rejected in 50-100ms) and the leading hypothesis is the 45° facing arc — but that could not be CONFIRMED because the angle was computed here and thrown away, leaving only distance to correlate on (which cleared the range hypothesis but not this one). Now every cast states the distance, the reach, the off-by angle and the skill's arc, so the next cast-fail correlation is a grep. Info, not Verbose: this is the field needed to close a P0, not per-frame noise.
- **src/Fiesta.Bot/Manager/BotManager.cs:760** — Not in battle mode (or not yet told). Re-assert, but at most every 500ms so a burst of casts does not produce a burst of change-mode requests while the broadcast is still on the wire.
- **src/Fiesta.Bot/Manager/BotManager.cs:790** — <summary>Dismount if mounted, and WAIT for the server's RIDE_OFF. Returns true once not mounted. <para><b>Why this exists:</b> a gate CANNOT be used while mounted — the server silently ignores the click. Proven live 2026-08-04: mounted gate-use aborted after 14s of retries every time, while unmounted gate-use transitioned in ~150ms (18:58:19 mounted → abort; 18:59:52 and 18:59:57 unmounted → arrived in 153ms/156ms). That silent refusal was looping the whole travel system — the bot mounted for speed, failed the gate, re-routed, re-mounted, forever — which is why it went 30+ minutes without a single fight while exp went backwards from deaths in transit.</para> <para>The mount item is a TOGGLE (<c>NC_ITEM_USE_REQ</c>), and <c>IsMounted</c> only clears when the server's RIDE_OFF lands — so firing twice re-mounts. Issue once, then wait for the ack.</para></summary>
- **src/Fiesta.Bot/Manager/BotManager.cs:1014** — <summary>How long to wait for NC_BAT_TARGETINFO_CMD to confirm a fresh selection before sending BASHSTART anyway. One round trip on this server is single-digit ms; 60 leaves generous headroom and bounds the cost of a lost ack at 60ms once per engagement.</summary>
- **src/Fiesta.Bot/Manager/BotManager.cs:1019** — <summary>Wait (briefly, bounded) for the server to CONFIRM a freshly-sent target selection before acting on it. Returns whether confirmation arrived. <para>⛔ Neither BASHSTART nor a skill cast carries a target — both act on whatever the server currently has selected. So firing in the same millisecond as the TARGETTING that is meant to change that selection acts on the PREVIOUS one, which after a kill is a CORPSE, and the server refuses it. Measured on FighterFresh: a live Marlone Archer 17u away, 1 degree off our nose, reach 51 — and 0x0FCA "out of casting range" every 450ms for as long as it kept trying, while it lost 787 → 332 HP to two mobs it never damaged. The geometry we print is measured against the mob we INTEND; the server was still holding the dead one.</para> <para>Bounded, so a lost or unparsed TARGETINFO can never wedge an attack: on timeout we proceed exactly as before.</para></summary>
- **src/Fiesta.Bot/Manager/BotManager.cs:1054** — ── FIX 3 of 3: DO NOT RE-BASH INTO OUR OWN WINDUP ──────────────────────────────────────── CEASE_FIRE is an INTERRUPT, not a failure: for the real player the swing stream resumes on its own after 46% of them, with no new BASHSTART. We re-bashed at a MEDIAN 355ms — inside the 418ms windup — in 80% of cases, restarting the windup from zero each time and guaranteeing the stream never produced anything. 150 bashes -> 30 landed swings (0.20/bash vs the player's 5.96). Re-issuing on the SAME target inside the windup can only ever destroy progress, so refuse it. (A target CHANGE is different and still goes through — that is a real new engagement.)
- **src/Fiesta.Bot/Manager/BotManager.cs:1082** — ⛔ WAIT FOR THE SERVER TO CONFIRM THE SELECTION BEFORE BASHING (operator 2026-08-12: "from a player's perspective it would make sense if bashing before TARGETINFO should always fail ... seems like a free thing to try, costs at most 20ms"). BASHSTART carries NO target — it swings at whatever the server currently has selected — so a bash processed before our TARGETTING commits attacks the PREVIOUS selection, or nothing, and dies silently. We were sending TARGET, CHANGEMODE, STOP and BASHSTART inside ONE MILLISECOND and the TARGETINFO answering that selection arrived 23ms LATER (measured on the wire, 19:01:05.107-.131, on a bash that produced no swing at all). ⚠️ I briefly called this hypothesis refuted, wrongly. An ALIVE bash also had a 1ms send gap, so the gap "did not correlate". But the send gap and the TARGETINFO round-trip are both noisy PROXIES for the thing that matters — whether the server committed the selection before it processed the bash — and a 23ms ack can be a 1ms commit plus a spiky return path. Absence of correlation in a proxy is not refutation of the mechanism. Bounded, so a lost or unparsed TARGETINFO can never wedge the bash: we wait at most TargetConfirmWaitMs and bash anyway, which is exactly the old behaviour. Cost when it works is one round trip; cost when it does not is nothing.
- **src/Fiesta.Bot/Manager/BotManager.cs:1110** — FACE the mob then STOP, exactly like a skill cast (CastAsync) — the server validates a swing against facing, and a zero-distance moverun (when already adjacent) does NOT set facing, which is why BASHSTART produced no swings. FaceAndStop always steps a little toward the target so facing is correct. The CALLER must already be within melee weapon range (walk there first); this doesn't close large gaps. ⛔ …BUT NOT ON A RE-BASH MID-FIGHT. FaceAndStop's MOVERUN is exactly what breaks a swing stream ("MOVING breaks the swing stream, a bare STOP does not" — CastAsync's own note), so sending it unconditionally means every re-bash cancels the bash it is starting. Wire proof (Bot7170, RouVal02, 2026-08-05 08:22): re-bash → MOVERUN → CEASE_FIRE → re-bash, five "was ACTIVE" cancellations in three seconds (sessions 258→266), halving our damage output in the fights that were killing the bot. A CONNECTING HIT in the last 2500ms already proves we are in range AND faced — the server only lands our swing when both hold — so a re-bash then needs the STOP alone. This is the same signal, and the same bootstrap argument, that NeedsFacingAdjust uses on the cast path: the OPENING bash has no recent hit, so it still gets the full face+stop, lands a hit, and from then on the MOVERUN is suppressed and the stream persists.
- **src/Fiesta.Bot/Manager/BotManager.cs:1131** — ⛔ …AND THE BOOTSTRAP ARGUMENT ABOVE DOES NOT HOLD. It assumes the opening bash lands a hit, after which the MOVERUN is suppressed and the stream persists. Measured on ClericFresh over 18.5 minutes in Job1_Dn01 (tools/swings_per_bash.py): 374 BASHSTART, 92 swings — 0.25 swings per bash against a real player's 5.96 — 537 CEASE_FIRE, and a dead bash cease-fires at a MEDIAN OF 70ms. `facedByRecentHit` could fire on only 20% of those bashes, because it asks for a connecting hit in the last 2.5s and a connecting hit is precisely what the bot cannot get. An escape hatch whose precondition only the blocked action can produce never opens. So ASK THE GEOMETRY INSTEAD, which needs nothing to have gone right first: if our heading is already pointed at the target, the face-step is a no-op that can only cancel the bash it precedes, and a bare STOP is enough. This is the same angle the target view surfaces (GET /api/bots/{id}/target → angleOffDeg). ⚠️ HONEST LIMIT ON THE EVIDENCE: bashes that skip the MOVERUN succeed far more often (85% vs 10%), but that split is exactly the facedByRecentHit split, so it cannot separate "the MOVERUN breaks the bash" from "a fight that is already landing hits keeps landing them". What IS established without that confound: the guard almost never fires, and the codebase's own note says the MOVERUN it fails to suppress is what breaks a swing stream. Both readings point at the same fix, so it is worth making; the metric to watch afterwards is swings-per-bash. FaceOkDeg is bot-behaviour tuning (like melee range or tick cadence), not a game fact — the server's real facing tolerance is unknown, so this stays inside the 22.5 degree half-arc that every melee skill declares as its UsableDegree, and the angle is logged so it can be tuned.
- **src/Fiesta.Bot/Manager/BotManager.cs:1168** — ⛔ NEITHER BRANCH RAN = WE BASH WHILE STILL MOVING. If the target is not in NpcPos (not in the mob or player maps — AoI flicker, a handle we learned elsewhere), the old code fell through and sent BASHSTART with no STOP at all. The server then cease-fires within ~47ms, before we send anything else: measured post-fix, that is what remains of the dead bashes. A committed STOP is the one thing every successful bash in the player capture has.
- **src/Fiesta.Bot/Manager/BotManager.cs:2013** — Wait long enough for the WHOLE path to walk — a valid cross-map route can be thousands of world units (EldGbl02→gate ≈ 4200u ≈ 60s). A fixed 30s aborts a legit long walk mid-route, churning re-plans until an upstream stall-timer gives up. Scale the wait to the path length at the bot's live walk speed (+60% slack for pacing/detours), floored/capped to sane bounds.
- **src/Fiesta.Bot/Manager/BotManager.cs:2116** — Don't USE at full HP — the server rejects it (USEFAIL at 100% HP), which both wastes the call and falsely reads as an empty reserve. Skip as a no-op when already full.
- **src/Fiesta.Bot/Manager/BotManager.cs:2120** — ⛔ DO NOT GATE THE HEAL ON THE LEARNED COOLDOWN. An earlier version of this method did exactly that and it KILLED THE BOT (2026-08-05, 16:29:09, -264 exp): 16:29:08.038 HP stone still on cooldown — 5181ms to go (learned cd 12834ms); NOT firing. 16:29:09.653 DEATH -264 (hp 9%, 11 aggressors) The estimator takes the MINIMUM gap between successful uses, so it converges on the true cooldown FROM ABOVE — early in a session it OVERESTIMATES (12834ms here vs the ~6900ms measured once converged). Using an UPPER bound to BLOCK an action is unsafe by construction: it withholds a heal that was very likely available, and at 9% HP that is fatal. To block safely we would need a LOWER bound on the cooldown, which nothing here provides. So: always fire. A refused USE costs one packet; a withheld heal costs the character. The spam is cosmetic, the missed heal is not. HpStoneCooldownMs / HpStoneReadyInMs remain exposed for the driver to REASON with ("I probably cannot heal for Nms, so do not take this fight") — informing a decision is fine, silently suppressing the action is not. NOTE the wording: this logs the REQUEST, not a heal. It used to say "recharge", which read as success — during the 15:30:41 death it printed five times while every use was refused and HP fell 628->0. The outcome is the 0x5008/0x5006 ack, logged by ZoneView.
- **src/Fiesta.Bot/Manager/BotManager.cs:2913** — ⛔ REPORT WHERE WE *ARE*, NOT WHERE WE ARE GOING (measured 2026-08-13). BeginMove returns the INTERPOLATED point we occupy right now and starts the segment in one atomic step, so `from` is always the true current position — including when a re-path interrupts a walk half a step in (operator: "if we walk from A to B and we're 0.5 sec into it, and change path to C, do we send a packet from 0.5*(A+B) towards C, or from B? Because that WOULD movefail"). It does. Measured on real-client captures: a real client's `from` equals the previous `to` on only 2% of frames (Z:/LongCaptureNoDc.pcapng, n=20337) / 16% (Z:/JCQMany.pcapng, n=403); ours was 48% (MageFresh) / 90% (JcqArcher), and the server rejected 44% of our moves vs 0.25-0.6%. Pace using the bot's live MOVESPEED-tracked walk speed (fallback: the passed value).
- **src/Fiesta.Bot/Manager/BotManager.cs:2953** — <summary>Walk from one map coordinate to another (one MoverunCmd step). <para>⛔ SUPPRESSED WHILE A CAST BAR IS OPEN. Moving cancels a server-side cast, and this is the single choke point every MOVERUN goes through — so gating here covers the nav re-path, the travel driver and the Lua tick at once. Proven cause of the mount never landing: the summon's own root arrived as a MOVEFAIL, nav called it a desync and re-pathed 71ms later, and the server cancelled our summon (see <see cref="ZoneView.CastBarActive"/> for the packet trace).</para></summary>
- **src/Fiesta.Bot/Manager/BotManager.cs:3091** — <summary> Delay between confirming a cross-server zone handoff (NC_MAP_LINKOTHER) and opening the connection to the new zone server. The WM tells the destination zone "expect this client" as part of the switch; connecting BEFORE that notification lands races the WM and the zone drops us (the intermittent "zone link breaks on a gate" under cluster load, operator 2026-07-29). <para><b>MEASURED AGAINST THE REAL CLIENT (Z:/GateTest.pcapng, 2026-08-04).</b> The operator captured ~15 round trips across a cross-node zone boundary (zone0 9016 ⇄ zone1 9019) in four conditions — cross-node mounted/unmounted and same-node mounted/unmounted — annotating "Not a single dc", "No dc", "0 dcs. Leaving it here." The client's gap from the OLD connection's last packet to the NEW connection's SYN, n=30: <b>median 0.442 s, min 0.331 s, max 0.527 s</b> — a very tight distribution, and every one of the 30 crossings succeeded.</para> <para>So the WM→zone notification reliably lands well inside ~0.5 s, and the original "a couple of seconds" was ~4x more conservative than anything the real client does. Waiting longer is not free: it leaves a multi-second hole where neither zone owns the session, which is a plausible source of the very drops it was meant to prevent (operator's hypothesis). Set to 600 ms — just above the client's observed WORST case (527 ms), so we stay inside real client behaviour with a small margin rather than inventing a value.</para> <para>⚠️ If gate-break DCs return, do NOT simply raise this again — re-measure against a fresh capture first. The client proves ~0.5 s is sufficient; a regression here means something else.</para> </summary>
- **src/Fiesta.Bot/Manager/BotManager.cs:3115** — <summary>Liveness watchdog cadence and the "this is definitely wrong" stillness threshold. 45s is well beyond any legitimate pause (a hand-in dialogue, a cast, waiting out a stun) but far short of the 15+ minutes the bot silently wasted before this existed. Bot-behaviour timing only.</summary> How long to wait after a CEASE_FIRE before deciding the swing stream is genuinely dead. The real client does not re-bash on a cease-fire at all (27 of 39 resumed by themselves — CombatPriest.pcapng), so this is a stall detector, not a cadence. Long enough to let the server's own resumption land. <summary>Swing windup: BASHSTART -> first landed damage, MEASURED median 418ms on our own logs. A cast or a re-bash inside this window destroys the swing that was about to land.</summary>
- **src/Fiesta.Bot/Manager/BotManager.cs:3315** — FIELD .sbi DOOR STATE FROM THE PUZZLE BOARD (operator 2026-08-18). A field door's closed-state is never sent to a late-joiner, so the bot used to learn it only by MOVEFAILing into the wall 6-12 times — ~30-60s of thrash on every visit, and the wedge that produced FighterFresh's 22.7% reject rate. The puzzle BOARD is a positive signal available the instant it comes into view: it is spawned by the same script init that builds the door, so board-in-a-door-box ⇒ that door is shut. Runs on entity refresh; NotePuzzleEntities is a single id compare per entity and idempotent.
- **src/Fiesta.Bot/Manager/BotManager.cs:3348** — 📊 Learned scalars (HP-stone cooldown + heal size). These need REPEATED observation — the cooldown is the min gap between two successful heals — so without persistence the survivability inequality is unavailable for most of every session (measured: 8 minutes in, still unlearned, SustainableHealDps still -1). Seed, then keep learning.
- **src/Fiesta.Bot/Manager/BotManager.cs:3360** — Attack range survives the handoff now. It reset to 0 on every cross-server transition, and since the bot closes to ~1u before swinging, the first hit after a reset pinned it at ~2u — after which the cast path judged nearly everything OUT OF RANGE. Max ⇒ seed the max. ⛔ SEED THE MEAN, NOT THE MAX. The max latches ONE position-desync outlier and, being persisted, makes it permanent: every character on this server had meleeRange pinned at 135-150u (ClericFresh 149.2, Elfyra 149.8, MageFresh 149.7) — just under the 150 seed guard, so it sailed through on every login and overwrote the in-session learner that was specifically fixed to resist exactly this. What it cost: `tooClose` is 0.40 x range, so a 149u range made the bot treat 60u as TOO CLOSE. ClericFresh — a near-unkillable physical melee — was logging "TOO CLOSE (17u) — stepping BACK to ~104u standoff" and retreating OUT of melee, unable to kill anything while the mobs kept hitting it. The mean over hundreds of connects is robust and, crucially, CORRECT: 50.3 (n=1010), 51.0 (n=427), 50.9 (n=349), 52.4 (n=171), 50.8 (n=87). That independently reproduces the real-client capture measured with tools/client_range.py — auto-attack median 49u, Range=0 melee skills median 51u. Two unrelated sources, same answer; the max agrees with neither.
- **src/Fiesta.Bot/Manager/BotManager.cs:3620** — ⛔ NO AUTOMATIC RE-BASH. ONE BASH PER ENGAGEMENT (operator 2026-08-12: "I just want a singular initial bash on combat start"). What used to live here: a CEASE_FIRE handler that waited 1200ms and re-issued the full target/mode/face+stop/BASHSTART sequence if no damage had landed. It was written to fix a real symptom and it made things worse every time it was tuned, because the premise was wrong: CEASE_FIRE is a transient combat-state notification, not "your attack stopped". In the operator's own priest capture the server resumed swinging on its own after 27 of 39 cease-fires with no client BASHSTART at all — the real client bashes FIVE times and collects EIGHTY-TWO swings. Measured cost of re-bashing anyway, on ClericFresh: 374 BASHSTART in 18.5 min for 92 landed swings (0.25/bash), and when a second re-bash owner was briefly added, 1854 in 27.8 min for 71 swings (0.04/bash). Every re-issue restarts the 418ms windup from zero, so bashing harder produced strictly LESS damage. The real-player baseline is 5.96. The bash is now issued exactly once, by the driver, when it engages a target — and if the stream really does die, the fix belongs in whatever killed it (a cast's STOP, a walk), not in a restart that cancels the next swing too. CEASE_FIRE is still decoded and logged; that is observability, and it is how this was measured.
- **src/Fiesta.Bot/Manager/BotManager.cs:3687** — Standoff thresholds derived from the LEARNED attack range (operator 2026-07-15): measured from the wire (ZoneView.LearnedMeleeRange ≈ 48u for this fighter; an archer's is far larger), so the standoff is correct for ANY class/weapon instead of the baked 30/20. Fall back to the tuned consts until the first connecting hit teaches us the real range.
- **src/Fiesta.Bot/Manager/NpcKnowledge.cs:190** — <summary>Remember that a bot SHOULD BE RUNNING, so a pod restart can bring it back by itself. <para>⛔ THE OTHER HALF OF THE SAME HOLE. <see cref="SaveScript"/> persists WHAT a bot runs, but not THAT it exists — so after every restart the process came up with an empty roster and the whole test simply stopped, silently. Measured 2026-08-11: the host restarted FOUR times (deploys plus a node OOM) and each time `GET /api/bots` returned `[]` with nobody logged in and nothing saying so. An external watchdog script was standing in for this, which is not a fix — it only runs while a human is around to start it.</para> <para>⚠️ The spawn options carry CREDENTIALS, so this file is written to the knowledge PVC beside the other learned state and must never be logged or committed.</para></summary>
- **src/Fiesta.Bot/Manager/NpcKnowledge.cs:312** — <summary>Record (and persist) a death while pursuing this quest; returns the new total. <para>Exists because the Lua driver's own <c>questDeaths</c> table is a script-local, wiped on every script re-apply — and this bot re-applies/respawns constantly, so the count that was supposed to rank a lethal quest LAST never survived to do it. Measured 2026-08-05: right after shipping a death-ranked ordering, the bot went straight back to the mob that had killed it and spent 53% of a 6-minute budget walking to it. Knowledge that resets every session cannot influence a bot that restarts every few minutes — so it lives here, on the PVC.</para></summary>
- **src/Fiesta.Bot/Manager/NpcKnowledge.cs:395** — ⚔️ DURABLE MOB THREAT TABLE — how hard each mob hits, learned by being hit. ZoneView learns this per SESSION, but ZoneView is recreated on every respawn/pod-roll, and this bot respawns constantly (deploys, deaths, cross-server handoffs). Measured 2026-08-05: mob4002 killed the bot TWICE (208-246 dmg vs 881 maxHp = four hits, -528 exp) and in the very next session was not in the table at all — so the lethality demotion built on it could not fire for the mob that motivated it. Each session re-paid the learning cost the model exists to avoid paying twice. Persisting it here means a mob is learned ONCE EVER, the same principle as the shop classification above. ⚠️ Stores the SAMPLES (max/count/sum), never a derived verdict like "dangerous": maxHp changes with levels and gear, so "how many hits can this kill me in" must be recomputed against CURRENT maxHp, not frozen at the moment of first contact.
- **src/Fiesta.Bot/Manager/NpcKnowledge.cs:471** — 📊 DURABLE LEARNED SCALARS — per-server measurements that take REPEATED OBSERVATION to establish and are therefore worthless if they reset every session. The HP-stone cooldown is the motivating case: it is derived from the MINIMUM gap between two successful uses, so a fresh session cannot know it until the bot has healed twice. Measured 2026-08-05: eight minutes into a session it was still unlearned and SustainableHealDps still read -1 — i.e. the survivability inequality was unavailable for most of the session, which is exactly when the bot is dying (deaths consumed 102% of exp over a 75-min window). Keeps Min/Max/Count/Sum so a caller can use whichever statistic is correct for its quantity: · cooldown → MIN (converges on the truth FROM ABOVE, so the smallest gap ever seen is the best estimate) · heal size → AVG/MAX Stores raw samples, never a derived verdict — same rule as the mob threat table.
- **src/Fiesta.Bot/Manager/NpcKnowledge.cs:490** — <summary>A learned scalar for this server, or null if never measured. NULL = NO EVIDENCE.</summary>
- **src/Fiesta.Bot/Metrics/MetricStore.cs:39** — <summary> A tiny in-memory metrics engine, one per bot: <c>InitMetric</c> once, <c>LogMetric</c> freely, then read windowed stats for a live "stat panel" view of the bot. <para>The point (operator 2026-08-05): <i>"a window into everything going on with the bot — like a stat panel, you look at it and immediately know where the bot is."</i></para> <para><b>Batching is what makes the write API safe to call anywhere.</b> Callers log at whatever rate their code happens to run at — a 400ms Lua tick, a per-packet handler, a burst of 20 hits in one second — and everything inside a metric's batch window collapses to ONE sample (averaged for a Gauge, summed for a Counter). So the ring buffer measures TIME, not caller frequency, and nobody has to think about tick rates. Without it a chatty caller would flush the buffer in seconds and a quiet one would span hours, making the same "1m avg" mean completely different things per metric.</para> <para>Thread-safe: packets arrive on the session read loop while HTTP reads the snapshot.</para> </summary>
- **src/Fiesta.Bot/Metrics/MetricStore.cs:54** — <summary>Ring capacity per metric. At the default 500ms batch this is ~8 minutes of dense history, and far more for sparse event metrics — enough for the 1m/5m/10m windows below with headroom.</summary>
- **src/Fiesta.Bot/Metrics/MetricStore.cs:186** — Report the tail that MATTERS for this metric's direction: for HigherIsBetter the bad case is the LOW end ("95% of the time HP was at least X"), for LowerIsBetter it is the HIGH end ("95% of the time damage taken was at most X"). Reporting the wrong tail makes the panel look fine while the thing you care about is the other end of the distribution.
- **src/Fiesta.Bot/Navigation/KiteCircle.cs:31** — <summary>Required loop DIAMETER as a multiple of the chaser's attack range. <para>Started at 4x on the operator's first estimate, then MEASURED against the terrain we actually fight on (tools/kite_fit_probe.py) and lowered to 2.5x on their call. The measurement is why: the JCQ clone room (Job1_Dn01) holds a largest walkable circle of r=524u — a 1049u diameter — while 4x against the clone's ~400u range demands 1600u. No circle could ever fit, and 81 of 82 fits failed there. At 2.5x the floor is 1.25x range, so that same room supports an enemy range up to ~419u and the clone fits with a little margin.</para> <para>Lower is a real trade: the tighter the loop, the more of its time the chaser spends in range rather than travelling. 2.5x is the widest the venue allows, not the widest that would be good.</para></summary>
- **src/Fiesta.Bot/Navigation/KiteCircle.cs:44** — <param name="maxDiameter">Upper bound on the loop size in world units.</param> <param name="enemyRange">The chaser's attack range. The loop's DIAMETER must be at least <see cref="DiameterPerRange"/> x this, or the manoeuvre is pointless (operator 2026-08-13): on a tight loop "enemy just needs to take small adjustment steps to attack, so its 'attacking time' will be like 90%". Wide enough, it has to spend most of its time travelling instead — the goal is "it can only hit us like 20% of the time or so".</param> <param name="leeway">How far OUTSIDE the rim the bot may start. It only has to "almost contain" us (operator 2026-08-13) — a loop we are a little outside of is still cheap to ride onto, and insisting on strict containment throws away good circles. The ENEMY deliberately does NOT have to be inside: an earlier version required that and the operator corrected it.</param> <returns>(CentreX, CentreY, Radius) of the largest fitting circle, or null when the ground will not hold one that satisfies the minimum — the caller then falls back to the straight-line kite (and should say so, because that kite ends at the first wall).</returns>
- **src/Fiesta.Bot/Navigation/KiteLoop.cs:5** — <summary> Builds a WALL-HUGGING kite loop: the outer contour of the room we are standing in, with narrow corridors removed, as an ordered ring of world waypoints. <para><b>Why this replaced the inscribed circle</b> (operator 2026-08-13). A circle is capped by the room's SHORT axis, and worse, at radius ~= the chaser's range it is actively pointless: <i>"range = radius means we just circle but the enemy never has to move => no effect from kiting"</i> — the chaser parks in the middle and covers the whole rim. Measured, the JCQ clone room holds a largest circle of r=524u while the mage needed 562u, so it fitted 0 of 15 attempts. Hugging the wall instead uses the room's full extent and keeps the greatest possible separation from anything inside it.</para> <para><b>Corridors are skipped by construction</b>, per the operator's "detection of narrow corridors that attach to it and skipping those": every cell whose clearance is below <paramref name="corridorMinWidth"/>/2 is deleted BEFORE the contour is traced, so a side passage is simply not part of the shape. That also removes the hard turn a corridor would force — the thing that makes linear kiting deadly ("during this turn you will be in enemy range for like 10 secs").</para> <para>Pure BYO nav geometry from the <c>.shbd</c>, like CoveragePath — no ids, no coordinates.</para> </summary>
- **src/Fiesta.Bot/Navigation/KiteLoop.cs:79** — 2. KEEP only cells wide enough to be "room" rather than "corridor". Half the required width, because clearance is measured from the centre outward to the nearest wall.
- **src/Fiesta.Bot/Navigation/KiteLoop.cs:114** — ⛔ RE-TAKE THE COMPONENT AFTER INSETTING. The inset can SPLIT the room into several blobs, and the contour trace starts at the first filled cell in raster order — which is then some stray fragment, not the room we are standing in. Measured: this produced a 5-waypoint loop spanning 6x6 world units, i.e. a single tile, in a room hundreds of units across.
- **src/Fiesta.Bot/Navigation/KiteLoop.cs:189** — ⛔ START POINTING SO THE FIRST BACKTRACK PROBE IS *WEST*. The start cell is topmost-leftmost, so west is guaranteed to be outside the shape. With dir=0 the probe starts north-west instead and the trace closes on itself almost immediately — measured, it produced a 5-cell "loop" spanning one tile in a room 1400 units across.
- **src/Fiesta.Bot/Net/PacketLog.cs:36** — <summary>Record OUR OWN entity handle in the log, so analysis never has to infer it. <para>⛔ WHY THIS EXISTS: SWING_START/SWING_DAMAGE are BROADCASTS about every nearby entity, and this server runs five bots plus real players on the same maps, so several handles legitimately attack the same mobs. Tools were guessing which one was us ("attacker of the first swing after our bash", "whoever damages what we targeted") and getting it WRONG — a log where we are plainly h8300 resolved to h8303. With the wrong handle our own swings are invisible and every bash scores as DEAD, which is exactly how a "90% dead bash" statistic was produced for a bot that was swinging normally. The bot has known its own handle since login; nothing should be inferring it.</para></summary>
- **src/Fiesta.Bot/Pathfinding/BlockGrid.cs:3** — <summary> A map's walkability grid, loaded from a server <c>.shbd</c> (Shine Block Data) file under <c>9Data/Shine/BlockInfo/&lt;Map&gt;.shbd</c> (BYO at runtime). Format (recovered + calibrated live 2026-06-11): 8-byte header = two LE u32 [bytesPerRow, height]; then <c>height</c> rows of <c>bytesPerRow</c> bytes, <b>1 bit per tile</b>, LSB-first within a byte. <b>bit 0 = walkable, bit 1 = blocked/void.</b> The playable map is a small region (~7% of tiles) inside a large void of bit-1 padding. <para>World↔tile is a plain linear scale with <b>no offset and no Y-flip</b>: a Shine map-unit is 50 world units and the grid stores 8 tiles per map-unit, so each tile spans 50/8 = <see cref="WorldPerTile"/> = 6.25 world units. The grid therefore covers world [0, tiles·6.25] (Eld: 4096·6.25 = 25600 = 512 map-units · 50). Verified exactly against Eld live — every gate and landmark lines up. tileX = worldX / 6.25 tileY = worldY / 6.25 walkable = ((row[tileX >> 3] >> (tileX &amp; 7)) &amp; 1) == 0 </para> </summary>
- **src/Fiesta.Bot/Pathfinding/BlockGrid.cs:253** — ── FIELD .sbi DOOR STATE FROM THE PUZZLE ENTITIES (operator 2026-08-18) ─────────────────────────────────── The MOVEFAIL learning above is a CRUTCH: it only concludes "closed" AFTER bouncing off the wall 6-12 times, which costs ~30-60s of thrash every visit. The puzzle pieces themselves are a POSITIVE signal that is true the moment they come into view — the mini-game only runs while the door is shut, so a piece standing inside an .sbi door box means that door is closed, with no MOVEFAIL needed at all. ⚠️ AUTHORISED HARDCODE (operator 2026-08-18: "Hardcode the 3 spawned puzzle mob ids" → "Just do 03 and 04 (the 2 puzzle types)"). The structural rule I proposed first — "any mob inside a door box" — was REJECTED by the operator as a false-positive risk, and rightly: one wandering mob would wall an OPEN courtyard and send the bot the long way round forever. Ids read from MobInfo (ServerSource). The puzzle block is contiguous 15000-15035: 15000/15001/15002 Xiaoming / Aruming / Toriming — the door NPCs 15003 PzlSlimeFull · 15004 PzlHoneyFull — the completed-picture display, one per puzzle type 15005-15034 PzlSlime1_1..4_3 / PzlHoney1_1..4_3 — the individual sliding tiles 15035 PzlBoard_4x4 — the board (PRIMARY signal) Spawn coords come from AIScript/Toryming.lua and all sit dead centre of the Toryming door box (16150,14350)-(16844,14694): boards at (16324,14522) and (16664,14522), the two Full pictures co-located with them, the 30 tiles across x16249-16739 / y14447-14597. So every candidate is genuinely inside. ⭐ MATCH THE BOARD AND ONLY THE BOARD (operator 2026-08-18: "Actually 35 might be best, if it's actually inside the door bounds?" — it is; then "Use only 35 (empty puzzle frame) will include future puzzles too"). Three reasons it is the right single id: 1. STABLE for the whole closed period. The boards are regen'd by the same init loop that calls cDoorBuild (Toryming.lua:370), so a board exists exactly as long as the event — and therefore the wall — does. 2. The Full pictures are NOT stable: PG_COMPLETE_SUCCESS_GAME re-spawns one when a puzzle is SOLVED (Toryming.lua:1274), so 15003/15004 track play state, not door state. 3. GENERALISES. PzlBoard_4x4 is the empty frame shared by BOTH puzzle types (one id, both boards), so any future puzzle built on the same frame is covered without touching this list. The door NPCs (15000-15002) are deliberately EXCLUDED — they are always present regardless of door state, so they identify WHICH door a box belongs to but say nothing about whether it is shut. Measured live: all three presented byte-identical state (mode=2 type=6 huntable=false level=0) while Toryming was closed.
- **src/Fiesta.Bot/Scripting/BehaviorGraphRunner.cs:68** — <summary>Request a transition to <paramref name="state"/> on the next tick (operator flip / <c>bot.requestState</c>). Fires even with no defined edge (a forced switch) and <b>pins</b> the graph there — autonomous transitions are suppressed until you request the special state <c>"auto"</c>, which resumes autonomous behaviour from the current state. (So a deliberate flip to e.g. stay_alive holds for "90% control".)</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:87** — <summary>Milliseconds until this skill can be cast again — 0 means READY. Cooldown and cast time come from client data, so the caller passes nothing but the id. <para>⭐ THIS IS THE SERVER'S CLOCK, NOT OURS. It runs from the CONFIRMED start (0x244E / CAST_SUC_ACK), which is the only moment a cooldown actually begins. A driver timing from its own SEND cannot work: the confirmation is a round trip away, so at the instant of sending it has not arrived and the driver must either bank a cooldown for a cast that may never have happened, or bank nothing and re-press.</para> <para>MEASURED COST OF NOT HAVING THIS (MageFresh, 2026-08-13): the leveler banked its cooldown only when <c>castConfirmed()</c> was already true at the send — essentially never — so every cast entered a re-press window that blocked the WHOLE rotation for several passes. Result: 15 casts in a 75-minute buffer, only 3 distinct skills ever used, and a median 1.8s between casts for a class that should empty its bar in 1-2s. Ask this instead and a skill that has started is simply not ready, so the next pass picks a different one.</para></summary> <summary>Mean FINAL damage this skill has actually landed, or -1 with no samples. <para>⭐ RANK BY THIS, NOT BY THE CLIENT'S `damage` COLUMN. That column is a CONTRIBUTION over a shared weapon/magic base (operator: "skill damage is weapon damage — or mdmg depending on skill — plus the skill damage"), so it exaggerates the gap between ranks enormously. MEASURED on MageFresh: skill 6003 carries `damage` 176 and lands ~104-130; 6002 carries 112 and lands ~110. The table says 57% apart; the wire says under 15%.</para> <para>That difference decides the open question of whether Magic Missile rank 1 (1.8s cooldown) out-damages rank 4 (4s) on THROUGHPUT — the operator's rule being that a shorter shared cooldown wins whenever the final damage is above ~50%. Pair with <see cref="skillDamageSamples"/>: -1 means UNKNOWN and must be EXPLORED, never treated as weak.</para></summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:245** — <summary>Record a metric sample from the driver (the operator's <c>LogMetric("HP", v)</c>). Counters sum within the batch window, gauges average — an unknown name auto-registers rather than dropping the data. This is how QUEST-LEVEL facts get measured at all: whether a quest was given up, deprioritized or completed is a decision only the Lua makes, so without this the epic's quest metrics were unreachable from C#.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:318** — <summary>How many times we have DIED pursuing this quest, across ALL sessions. PERSISTED — use this to rank a repeatedly-lethal quest last, NOT a script-local counter: script locals are wiped on every re-apply and this bot respawns constantly, so a local count never survives long enough to influence the choice it was written for (measured 2026-08-05).</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:581** — <summary>A generic "roomba" COVERAGE PATH over the current map's walkability (<c>.shbd</c>): an ordered list of world waypoints ({ x, y }) laid on a serpentine lattice of spacing <paramref name="stepWorld"/>, snapped to walkable ground. Walk them in order and the bot sweeps the whole walkable area — every mob comes into AoI (<c>nearbyMobs()</c>) so the instance/KQ hoover can clear it. <paramref name="stepWorld"/> is the sweep spacing (bot-behaviour tuning); keep it ≤ the server mob AoI for full coverage (smaller = safe, just more points). Empty if no grid for this map. Pure BYO nav geometry — no baked ids/coords.</summary> <summary>Fit the largest WALKABLE circle to kite around, from the map's <c>.shbd</c>: <c>{cx, cy, r}</c>, or nil when the ground will not hold one (caller falls back to a straight kite). Cached per map+area because the fit scans the grid and the terrain does not move.</summary> <summary>The WALL-HUGGING kite loop for the room we are in: an ordered ring of {x,y} world waypoints with narrow corridors excluded, or an empty table when the room will not yield one. Cached per map + coarse position, because the terrain does not move. <para>Replaces the inscribed-circle fit, which was capped by the room's short axis and degenerate when its radius approached the chaser's range (the chaser parks in the middle and never moves). Measured in the JCQ clone room: the best circle was r=524u / 3293u circumference, while the wall loop spans 1425x1181u with a ~5414u perimeter.</para></summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1018** — <summary>Quests the bot should consider accepting, driven from QuestData.shn directly (NOT the server's QUEST_READ list — in these data files the low-level quests are buggily absent from it). A quest is eligible when: not already done, not already active, it has at least one kill objective (type==1), and it has a real start NPC. Class is not filtered (≈99% of quests are unrestricted). Level isn't hard-gated either — the server rejects an out-of-level accept, which the driver detects via <c>questActive</c> not flipping. Each row: {id, startNpc, turnInNpc, minLevel, repeatable, title, objectives[{mob,count}]}. The driver bulk-accepts these at their start NPCs in town.</summary> <summary>Maps a Fiesta class id to its base/first-job class (the LINE leader): Fighter(1-5)→1, Cleric(6-10)→6, Archer(11-15)→11, Mage(16-20)→16, Joker(21+)→21. Quest Class@63 is a base id; matching by line lets an advanced char (e.g. Warrior 3) still take its line's base quests.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1166** — <summary>How many of this mob's hardest hits it would take to kill us from full, or -1 if unknown. The direct survivability question: &lt;=3 means a single mob can burst us down, and quest targets like that need a different plan (or to be skipped) rather than a straight melee trade.</summary> <summary>HP restored by ONE soul-stone charge as the SERVER advertises it in the soul-stone shop packet (0x3C05); 0 until a healer's shop has been opened. Use this for the sustain model in preference to any measured figure — it is exact, needs no samples, and cannot be polluted by regen / potions / an ally's heal. Live example: 270 per charge on a level-16 character.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1177** — <summary>Mean HP restored by one soul stone, -1 if never measured.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1180** — <summary>Sustainable healing in HP/sec from the stone (mean heal ÷ learned cooldown), -1 if unknown. <para>⭐ THE SURVIVABILITY INEQUALITY. Compare against incoming DPS: if the pack deals more than this, the fight CANNOT be out-healed and no rotation tuning changes that. This is the term that was missing while the per-mob "hits to kill" check looked at the wrong statistic — measured over 75 minutes, the dominant death mode was PACK ATTRITION (one death was mob19 landing 58 hits of ≤13 damage, which "hits to kill = 68" calls perfectly safe), and deaths consumed 102% of all exp earned.</para></summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1217** — <summary>The learned HP soul-stone cooldown in ms (-1 until two successful uses have been observed). Derived from the wire, so it stays correct if the server's value differs from the ~6.9s measured.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1250** — <summary>Max HP soul-stone reserve capacity (from the [1802] param block), or 0 if not seeded. Lets a script restock at a percentage of capacity (e.g. &lt;10%).</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1290** — <summary>Learned effective attack range (max distance a swing of ours has connected at) — 0 until the first connecting hit. Measured from the wire since no client file / PDB carries it (operator 2026-07-15).</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1362** — <summary>True while a LOCAL walk (<see cref="BotManager.WalkPath"/>) is streaming move steps — the same-map counterpart to <see cref="traveling"/>, which only covers multi-hop CROSS-MAP routes. <para>⛔ Exposed 2026-08-13 because the driver's "arrived, so dismount" rule tested only <c>traveling()</c>. A same-map walk leaves that false, so the bot read a 1400u walk to an NPC as "arrived", dismounted 1.5s in, and covered the rest on foot at ~50% speed — then the mount toggle cooldown refused the re-mount (operator: "the mage is not mounting on town trip, just walking slowly along"). "Arrived" means no route AND no walk in flight.</para></summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1585** — <summary>Every current aggressor with its SPAWN ANCHOR and how far it has already been dragged from it (operator 2026-08-05: "for each aggro'd enemy track where it spawned to calculate how far it will chase"). Rows: {handle, mobId, x, y, anchorX, anchorY, fromSpawn, chaseLimit, willDropIn}. <para><c>fromSpawn</c> = how far this individual is from home right now. <c>chaseLimit</c> = the furthest any mob of its id has been observed to chase, LEARNED from the wire (0 = not measured yet). <c>willDropIn</c> = chaseLimit - fromSpawn, i.e. roughly how much further we must drag it before it gives up — negative/zero means it is already past everything we've ever seen this mob type do.</para> This is what makes a shed predictable per-mob instead of one global guess: each chaser has its own anchor, so a tail sheds progressively as each one runs out of leash.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1618** — <summary>Learned chase limit for a mob id — furthest from its spawn one has been seen while still aggroing. 0 = never measured. Read from the wire, never hardcoded.</summary>
- **src/Fiesta.Bot/Scripting/BotApi.cs:1729** — ⛔ NpcSeedAll, NOT NpcSeed. This says "every NPC+gate on the current map (authoritative)" and for gates it was delivering ONE PER MOB ID, because NpcSeed is a mob-id-keyed dictionary and every teleport gate shares mob id 32. So all but one gate were dropped, and the survivor's position got paired with whichever destination won the race — which is exactly the "bogus gate edge" the router keeps discovering the expensive way: [travel] hop 1/2: no gate to 'EldGbl02' in view near (10724,4318) — aborting [travel] PRUNED bogus gate edge RouVal02 -> EldGbl02 (gate not there) — will re-route ClericFresh burned 68% of a 38-minute budget (1549s) walking to gates that were not there, pruning, re-routing and repeating, at +0 exp. Same root as the map layer only drawing one gate per map; the nav graph was the other victim.
- **src/Fiesta.Bot/Session/ZoneView.cs:206** — <summary>⚠️ THE NAME IS A GUESS AND THE EVIDENCE CONTRADICTS IT. Kept only because call sites use it; treat the MEANING as "cast precondition unmet — probably MOVING / no committed STOP". <para>Measured 2026-08-06 by pairing every cast with its geometry: this code fires at <b>dist=1u</b> (three times). You cannot be "out of range" at one unit. Distance is not it.</para> <para>What DOES correlate is whether we committed a STOP first: <c>face+stop+cast</c> failed 2 of 9 (22%), plain <c>cast</c> failed 11 of 29 (38%). That matches the rest of this codebase's accumulated notes, which associate 0x0FCA with being MOVING or not having sent the STOP — never with distance.</para> <para>Same failure shape as 0x0FC0's old "dead / invalid state" label: an unverified name that reads like an explanation and steers every later investigation toward the wrong variable. I reasoned about distance for two passes because of this name.</para> <para>⚠️ UPDATE 2026-08-06 — the follow-up "missing STOP" theory is ALSO dead. Splitting the pre-cast paths three ways showed the no-STOP branch <b>never fires</b> (0 of 16 casts): every cast already commits a STOP, and 0x0FCA still fails ~50%. It cannot be explained by something that never happens. Two eliminations banked — NOT distance (fires at dist=1u), NOT missing-STOP — and no confirmed meaning yet. Do not add a third guess to this comment; test it.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:224** — <summary>The skill is NOT READY — still on cooldown (or a cast is already running). <para>Pinned from the wire 2026-08-05 (Bot7170, RouVal02). The cast at 07:55:44.42 SUCCEEDED — `0x2440 CAST_REQ` → `0x2435` + `0x244E HIT_OBJ_START` + three `0x2452 HIT_DAMAGE`. The re-presses 530ms and 1060ms later both came back `0x2434` with this code, and Bone Slicer [02] (15s cooldown) cast at :44.4 failed again at :50.4 — exactly its remaining cooldown. So this is the server saying "not yet", i.e. the EXPECTED answer to a spam re-press.</para> <para>⚠️ CORRECTED 2026-08-06 — the previous version of this note said a caller "must STOP re-pressing" because each re-press killed the swing stream. That was superseded by Z:/CombatPriest.pcapng (2026-08-04) and is no longer true: it is the <b>MOVERUN</b>, not the STOP, that breaks the stream, and <c>NeedsFacingAdjust</c> now suppresses the MOVERUN once a connecting hit proves we are already in range and faced — a re-press then sends only the STOP, which the real client also sends before every cast.</para> <para>So spamming this is DELIBERATE and operator-specified (2026-08-04: "start at Now + Cooldown - 50ms and spam the button 5x every 50ms"), and a rejection here is expected noise, not a fault. Measured 2026-08-06: 28 of 40 cast failures were this code, with no swing-stream loss. <b>Do not "fix" it.</b> Left uncorrected, this note reads as a bug report and invites exactly that — the stale-note trap the runbook warns about.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:428** — NC_BAT_CEASE_FIRE_CMD (Bat cmd 61 = 0x243D): {handle u16@0} — the server telling us an entity STOPPED attacking. When handle == SelfHandle this is the authoritative "your melee auto-attack (BASHSTART) is no longer running" signal, and without it the bot believed it was still swinging. PROVEN on the wire (packets-JcqFresh.log, 2026-08-04): BASHSTART at 14:52:10.907, then castRotation fired 5 skills in 18ms — each cast goes through FaceAndStopAsync, whose NC_ACT_STOP_REQ cancels the swing stream — and the server answered CEASE_FIRE(self) at .961/.962/11.009. The auto-attack lived ~50ms. Across that session: 43 BASHSTART vs 467 self-inflicted STOP, and in the 36s before a death the bot dealt damage ONCE while taking it 77 times (mob then regenerated, since regen needs only ~20s without incoming damage). 654 CEASE_FIRE arrived in one session, all previously undecoded.
- **src/Fiesta.Bot/Session/ZoneView.cs:454** — ===== SERVER-AUTHORITATIVE CAST-ANIMATION STATE (operator 2026-08-04) ===== The character is LOCKED for the duration of a cast, so a driver must not start another one while it runs. The client-side ActiveSkill.CastTime is only a PREDICTION — the server is the authority. Exact sequence, read off the real client in Z:/CombatExtensive.pcapng (identical for every cast): C-> 0x2440 SKILLBASH_OBJ_CAST_REQ S<- 0x243D CEASE_FIRE (the cast kills the melee bash — server-confirmed) S<- 0x244E SKILLBASH_HIT_OBJ_START cast ACCEPTED, hit sequence begins S<- 0x2435 SKILLBASH_CAST_SUC_ACK authoritative "cast succeeded" S<- 0x2457 SKILLBASH_HIT_BLAST S<- 0x2452 SKILLBASH_HIT_DAMAGE cast RESOLVES Measured START->DAMAGE windows in that capture: 154 / 129 / 63 / 21 / 178 ms.
- **src/Fiesta.Bot/Session/ZoneView.cs:557** — BRIEFINFODELETE as you move). Use ONLY for live in-view things (combat targets, the nearby gate HANDLE to click). For "where is NPC X on this map" use _npcSeed (below), not this. STICKY recently-seen mob cache (2026-07-15) — the fix for the instance AoI-flicker combat stall ([[fiesta-instance-roomba-coverage]]): in Job1_Dn01 a mob melee-hits us then 159ms later sends MAP_LOGOUT 0x1805 (leaves our AoI) then REGENs — the constant blink meant the bot DROPPED the target every flicker (BRIEFINFODELETE/MAP_LOGOUT hard-removed it), so it re-acquired/re-faced/re-cast slower than the flicker interval → 0 kills, casts fail out-of-range 0x0FCA on the departed handle. Fix: an AoI-leave (BriefDelete/MapLogout) moves a mob HERE (last pos + a short expiry) instead of dropping it, so combat keeps holding the target through the blink; a re-appear (AddOrUpdateNpc) or a real death (REALLYKILL) removes it. NearbyNpcs returns _npcs ∪ non-expired _recentNpcs. Value = (npc, expiryTick).
- **src/Fiesta.Bot/Session/ZoneView.cs:865** — <summary>Max soul-stone reserve charges (HP/SP), from the [1802] param block (MaxHPStone/MaxSPStone). 0 until seeded.</summary> <summary>HP restored by ONE soul-stone charge, as advertised by the soul-stone shop (<c>0x3C05</c>). 0 until a soul-stone shop has been opened this session. This is the SERVER'S own number — prefer it over any measured/inferred sustain figure, which cannot separate the stone's healing from regen, potions or an ally's heal.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1031** — 🏠 SPAWN ANCHORS — "for each aggro'd enemy track where it spawned to calculate how far it will chase" (operator 2026-08-05). A mob's leash is anchored to its SPAWN, not to us: it gives up at some radius from home, which is why running in a straight line sheds a tail PROGRESSIVELY (each mob hits its own limit at a different moment, because each has a different anchor). Distance-from-us can't express that. Where the anchor comes from — entirely off the wire, nothing baked: a mob WALKS (0x2018) when idle and RUNS (0x201A) when aggro'd (already decoded below). So its position while idle IS its spawn neighbourhood. We seed the anchor on first sighting, keep refreshing it while it walks (idle wander stays near home), and FREEZE it the moment it becomes a confirmed aggressor — from then on every step is measured from home. IdleConfirmed: we have actually SEEN this mob at rest (walking, or first-sighted while it wasn't already chasing us). Only idle-confirmed anchors are allowed to teach a chase limit — a mob first seen mid-run is anchored wherever it happened to be, which would inflate the learned leash with a number we never observed.
- **src/Fiesta.Bot/Session/ZoneView.cs:1044** — LEARNED per-mob chase limit: the furthest from its own anchor a mob of this id has been seen while STILL confirmed aggroing. A lower bound on its leash that only ever tightens upward as we observe more — the measured alternative to a hardcoded leash constant.
- **src/Fiesta.Bot/Session/ZoneView.cs:1064** — <summary>Furthest-from-spawn this mob id has been observed while still aggroing us (0 = never measured). LEARNED from the wire, never hardcoded.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1112** — <summary>When the server opened a cast bar on us (<c>NC_ACT_CREATECASTBAR</c>), UtcMinValue if none is in flight. Cleared by <c>NC_ACT_CANCELCASTBAR</c> and by whatever completes the cast (RIDE_ON for a mount summon). <para>⚠️ <c>NC_ACT_CANCELCASTBAR</c> (0x2048) is misleadingly named: it fires on COMPLETION as well as on a genuine cancel — observed arriving in the same millisecond as the RIDE_ON of a successful summon. Use the cast's DURATION to tell them apart (self-cancelled: 99ms; completed: 2755ms).</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1120** — <summary>⭐ True while a server-side CAST BAR is in flight — a timed action (mount summon, skill) that <b>MOVING CANCELS</b>. Gate movement on this. <para>Why this exists (wire trace, packets-JcqFresh.log 2026-08-05, mount summon):</para> <code> 14:59:12.422 C-&gt;S 0x3015 ITEM_USE_REQ (mount summon) 14:59:12.437 S-&gt;C 0x201B ACT_MOVEFAIL_CMD (server roots us FOR the cast) 14:59:12.441 S-&gt;C 0x2047 ACT_CREATECASTBAR (the cast begins) 14:59:12.512 C-&gt;S 0x2019 ACT_MOVERUN_CMD (WE walk — nav read the MOVEFAIL as a desync and re-pathed) 14:59:12.540 S-&gt;C 0x2048 ACT_CANCELCASTBAR (server cancels OUR OWN summon, 71ms in) </code> Neither castbar opcode was decoded, so this was invisible: the bot mounted 58 times in one session and we blamed "the summon is refused". It was never refused — we cancelled it ourselves, every time, by reacting to the cast's own root as if it were a pathing failure. <para>NOTE THE ORDERING: the MOVEFAIL arrives ~4ms BEFORE the CREATECASTBAR, so a guard on the MOVEFAIL handler cannot see the cast yet. The gate has to be on the OUTBOUND MOVERUN (see WalkAsync).</para> <para>Bounded by <see cref="CastBarMaxWait"/> so a cast bar whose completion packet we don't yet decode can never freeze the bot — it expires and movement resumes.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1140** — <summary>Upper bound on how long we will hold still for a cast bar. The measured mount/dismount animation is ~3.1s (21 pre-gate dismounts: min 3.03s, median 3.14s), so 4.5s covers it with headroom while guaranteeing we cannot wedge if a completion packet is missed.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1148** — ⚔️ LEARNED INCOMING DAMAGE PER MOB. The [dmgtaken] log line has resolved the attacker's stable MobId since 2026-07-29 explicitly as "a data point for the survivability model" — but nothing ever consumed it, so the model was never built and every sample was logged and thrown away. Why it matters (2026-08-05, two deaths in 90s on RouTemDn02, -528 exp): mob4002 hits for 208-246 against our 881 maxHp — FOUR hits kill us — with mob4001 adding 71-83 each and 6-8 aggressors. HP fell 480 -> 16 in 0.9s (~500 dps). No heal on a ~7s cooldown can cover that. The bot had travelled FIVE hops to reach those mobs. Nothing in quest selection could ask "can I survive this mob?" because the answer was never retained. Learned from the wire per the no-hardcoding rule — client MobInfo carries no attack stats, so observation is the only source.
- **src/Fiesta.Bot/Session/ZoneView.cs:1215** — <summary>Raised when a per-server scalar is measured, so it can be persisted. Args: (name, value). These take REPEATED observation to establish (the stone cooldown needs two successful heals), so without persistence they are unavailable for most of a session — measured: 8 minutes in, still unlearned, and the survivability inequality still reading -1.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1245** — <summary>Seed <see cref="LearnedMeleeRange"/> from durable knowledge. <para>Without this it RESET on every map handoff — each cross-server transition builds a fresh ZoneView. Measured 2026-08-06: <c>2u → 46u → 98u</c>, then a handoff and straight back to <c>2u</c>. Because the bot closes to ~1u before swinging, the first connecting hit after a reset pins the "range" at ~2u, and the cast path then reads almost everything as OUT OF RANGE → it fires the face+stop MOVERUN → which this codebase already documents as the thing that BREAKS the swing stream → fewer connecting hits → the range never recovers. Same shape as the mob threat table and the quest death counts: knowledge that resets every session cannot help a bot that transitions constantly.</para> <para>It is a MAX, so it converges from BELOW and seeding can only ever help — unlike the HP-stone cooldown (a min-gap that converges from ABOVE and must never be used to suppress).</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1288** — 💚 HOW MUCH ONE HP STONE ACTUALLY HEALS. The stone's USESUC (0x5008) is EMPTY — the HP arrives separately as an HPCHANGE (0x240E). So the heal amount is only knowable by pairing them: snapshot HP at the success, then attribute the next HP *increase* that lands within a short window. WHY THIS MATTERS: the dominant death mode is PACK ATTRITION, not burst. Measured over a 75-min window (6 deaths, exp NET -28 — deaths consumed 102% of everything earned): one death was `mob19` hitting us 58 times for max 13 each. Per-mob "hits to kill" says 68 hits and is useless there; what decides that fight is `incoming_dps vs heal_per_stone / cooldown`. We already learn incoming damage per mob and the ~6.9s cooldown — this is the last missing term.
- **src/Fiesta.Bot/Session/ZoneView.cs:1315** — <summary>Largest single-stone heal observed, -1 if never measured. ⚠️ CENSORED: a heal is clipped by missing HP, so this is a LOWER BOUND on the charge. Prefer <see cref="HpStoneChargePerUse"/>.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1323** — <summary>HP restored per stone charge, best available source first: the soul-stone shop's advertised figure (authoritative, needs no measurement), then an uncensored measured heal (equally exact), then the censored max (a lower bound, and the reason low-level bots under-reported sustain). -1 if none.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1331** — <summary>Mean heal per stone, -1 if never measured.</summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1336** — <summary>Sustainable healing in HP/sec from the stone alone: <b>MAX</b> observed heal ÷ learned cooldown. -1 until both have been measured. Compare against observed incoming DPS: if incoming exceeds this, the fight cannot be out-healed however the rotation is tuned. <para>⚠️ USES THE MAX, NOT THE MEAN, AND THAT IS DELIBERATE. A heal can never exceed MISSING HP, so every sample is really <c>min(stone_capacity, maxHp − hp)</c> — any heal taken at high HP is CLAMPED and understates the stone. Observed spread on one character: min 140, max 487 over 5 samples. The mean (377) is therefore biased LOW by the clamped samples, and an under-estimate of our own healing is the dangerous direction here: it would make the bot judge winnable fights unwinnable and walk away from them — precisely the "arbitrary cap that abandoned winnable fights" the operator removed 2026-07-16. The max is the best available estimate of true capacity (still a LOWER bound, since even it may have been clamped — it can only rise as more samples arrive).</para></summary> <summary>HP per second we can sustain from soul stones = one charge / the learned cooldown. <para>⛔ PREFERS THE SERVER'S ADVERTISED <see cref="HpStoneRestore"/> OVER THE MEASURED MAX, because the measured one is STRUCTURALLY TOO LOW. <see cref="HpStoneHealMax"/> is the largest observed <c>hpNow - hpAtStoneUse</c>, and a heal is CLIPPED BY MISSING HP — using a stone at 90% health can only ever show a small delta. So the measurement converges on the true charge only if the bot happens to heal from very low HP, and until then it under-reads, without ever looking wrong.</para> <para>That is the operator's 2026-08-11 P0 exactly: JcqArcher reported <b>11.41 HP/s</b> (~80 per charge / 7s) when its stones restore ~150+, and JcqMage <b>0.52 HP/s</b>. Both were "measurements", which is why they looked authoritative. The soul-stone shop states the real figure outright — 270 per charge on a level-16 character — so with the ~7s learned cooldown that is ~39 HP/s, not 11.</para> <para>Under-reporting sustain is the DANGEROUS direction for the survivability inequality: it makes winnable fights look unwinnable, which is what drove the flee → deprioritize → grind spiral.</para> <para>The measured max stays as the fallback for a bot that has not opened a healer's shop this session, and remains useful as a CHECK: if a measured heal ever exceeds the advertised charge, one of the two is wrong and worth investigating.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1383** — <summary>HP soul-stone cooldown in ms, LEARNED from the wire, -1 while genuinely unknown. <para>⛔ THE MINIMUM GAP BETWEEN SUCCESSES IS NOT A COOLDOWN MEASUREMENT. It only ever proves <c>cooldown ≤ gap</c> — i.e. it measures HOW OFTEN WE CHOSE TO HEAL, and it equals the cooldown only if the bot was actually spamming the stone. A bot that heals rarely learns a huge "cooldown" and reports a sustain figure near zero, which is true as a lower bound and useless as a capability.</para> <para>That is the operator's 2026-08-11 report exactly: FighterFresh learned <b>33098ms</b> (and earlier 71775ms) purely because a level-8 character in easy content seldom heals, and with a clipped 34-HP measured charge published <b>1.55 HP/s</b> — while ClericFresh, on the same build, sat at 57 HP/s. Nothing about the character differs by 40×; only the sampling did.</para> <para>So the cooldown is taken from the BRACKET both directions of the wire prove: every USEFAIL-with-charges-and-missing-HP gives <c>cooldown &gt; gap</c> (a floor), every success gives <c>cooldown ≤ gap</c> (a ceiling). With both, the midpoint is the best estimate. With only a floor, the floor. With only a ceiling it is UNCORROBORATED and we report -1 — an honest unknown the driver already handles ("sustainable heal UNKNOWN"), rather than a fabricated number it will make life-or-death decisions from. Under-reporting sustain is the dangerous direction: it makes winnable fights look unwinnable, which is the flee → deprioritize → grind spiral.</para> <para>A floor at or above the ceiling DISPROVES the ceiling (it was a stale durable seed, not a gap observed on this character) — the fail evidence wins. Live proof: MageFresh carried a seeded 3417ms while failing to heal at 3520ms and 4430ms since its last success, with 13 charges in reserve.</para></summary> <para>Now that the cooldown is a KNOWN GAME CONSTANT (hand-measured, 7s, identical for every character), this simply returns it. The per-character bracket is kept as a CHECK, not an override: every estimator available to us is noisier than the constant, so letting one outvote it can only make things worse — which is exactly how a level-8 fighter came to publish 1.55 HP/s. <see cref="StoneCooldownDisagrees"/> surfaces a bracket that genuinely contradicts the constant, which would mean the constant is wrong.</para>
- **src/Fiesta.Bot/Session/ZoneView.cs:1416** — <summary>Fallback HP soul-stone cooldown, used until this character's own wire evidence brackets it. <para>⚠️ HARDCODED GAME FACT — <b>operator-authorised 2026-08-11</b> ("I think the cooldown can be hardcoded at 7 seconds"), which the NO-HARDCODING rule permits only with that explicit approval. It is not in any client file: there is no HP soul-stone row in <c>ItemInfo.shn</c> at all, because the reserve is a server-side charge pool bought from the healer, not an inventory item — so <c>ItemAction.CoolTime</c> cannot be joined to it.</para> <para><b>7000ms — hand-measured by the operator, exactly 7 seconds.</b> That measurement is the authority here, and it corrects a bound I claimed from our own logs.</para> <para>⚠️ THE LESSON, because it will recur: I argued for 6000 on the grounds that the min success gaps (6.75s / 6.94s / 7.05s, measured 2026-08-05) put a "proven ceiling" at ≤6.75s, so 7000 was impossible. It was not proven at all. Those gaps are measured between <b>ACK ARRIVALS</b>, not between the server's cooldown starts, so a measured gap is <c>true_gap + (latency₂ − latency₁)</c> — a quarter second of ordinary jitter to a remote VPS renders a true 7000 as an observed 6750. A bound derived from noisy timestamps is an estimate wearing a bound's clothing. The FLOOR from USEFAILs (>4861ms) is unaffected and consistent with 7000; the gap between 4.9s and 7s simply holds no samples, because the bot retries in a burst right after a heal and then goes quiet — absence of evidence, which I read as evidence.</para> <para>Not in any client file: there is no HP soul-stone row in <c>ItemInfo.shn</c> at all, because the reserve is a server-side charge pool bought from the healer rather than an inventory item, so <c>ItemAction.CoolTime</c> cannot be joined to it. Hence a constant, with approval.</para> <para>A character's OWN bracket still overrides this once it has one — but note the bracket inherits the same jitter, so treat a bracket that merely straddles 7000 as agreeing with it.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:1571** — <summary>🧪 EXPERIMENT (operator 2026-08-11: *"Try hardcoding it at 50 to see what happens (likely ruins our archer's day but oh well)"*) — when &gt; 0 this OVERRIDES the learned range everywhere. <para>50u is the real client's measured melee reach: <c>tools/client_range.py</c> over <c>Z:/LongCaptureNoDc.pcapng</c> put a real player's auto-attack at median 49u / p75 61u, and the melee skills that declare <c>ActiveSkill.Range = 0</c> at median 51u. The learner meanwhile converged on 116–124u — roughly 2.4× reality — which is why it is worth testing whether the learner is the bug.</para> <para>⚠️ KNOWN CONSEQUENCE, accepted by the operator: this is a MELEE number applied to every class. The Lua's <c>engageRange()</c> detects a ranged weapon by testing <c>learned &gt; MELEE*2</c>, so forcing 50 makes that test fail and archers/mages will close to melee instead of standing off. Expect JcqArcher (currently the best-performing bot: 1% cast failures, 1:0.3 landed:taken) to get worse. That is the price of the experiment, not a regression to chase.</para> <para>❌ RESULT (2026-08-11, 25min live on 5 bots): <b>REVERTED — 50u made combat WORSE.</b> ClericFresh 0.36→0.13 swings/bash, 80%→95% dead bashes, 35%→56% cast failures (48 of them 0x0FCA); JcqArcher, as predicted, collapsed from 1:0.3 to <b>1:5.0</b> landed:taken with dead bashes 18%→85%. FighterFresh's failures did drop (70%→31%) but its swings/bash stayed flat, so nothing killed faster.</para> <para>The archer's predicted collapse is the CONTROL: it proves the override reached the standoff maths end-to-end, so the melee result is trustworthy too. <b>0x0FCA did not go away at 50u</b> — it got worse on the Cleric — so the inflated learned range is NOT the cause of the close-range cast refusals.</para> <para>Corroborating clue from the same window: MageFresh cast <b>161 times with ZERO failures</b>. Skills with a real ActiveSkill.Range never fail; 0x0FCA concentrates entirely on Range=0 MELEE skills. Next suspect stays the relative-position data feeding the distance, not the reach constant.</para> <para>Set back to 50 (or any value) to re-run; 0 = use the learner.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:2099** — <summary>The server CONFIRMED this skill started (0x244E NC_BAT_SKILLBASH_HIT_OBJ_START_CMD, which names the skill). This — not the moment we sent the request — is what a cooldown runs from. <para>⛔ THE DISTINCTION IS THE WHOLE BUG. A request that the server refuses (0x2434 err 0xFC8 "not ready") produces NO 0x244E, so timing from the SEND starts a phantom cooldown on a cast that never happened, and the driver then believes it is further through the rotation than it is. Measured 2026-08-11: 57 of the bot's 199 casts were refused for cooldown, against ZERO in an hour of real play — the real client gates locally and never transmits a doomed cast.</para></summary>
- **src/Fiesta.Bot/Session/ZoneView.cs:2112** — 🎯 MEASURED FINAL DAMAGE PER SKILL, paired cast→hit through the 0x244E/0x2452 `index`. ⛔ THIS IS THE ONLY HONEST ANSWER TO "WHICH SKILL HITS HARDEST PER SECOND". The client's ActiveSkill `damage` column is a CONTRIBUTION over a shared weapon/magic base (operator: "skill damage is weapon damage — or mdmg depending on skill — PLUS the skill damage"), so ranking by it is ranking by the wrong number: a low rank looks far weaker than it lands. Measured 2026-08-13, MageFresh: skill 6003 carries `damage` 176 and actually lands ~104-130, while 6002 carries 112 and lands ~110 — the gap in the table is 57%, the gap on the wire is under 15%. Ranking by the table therefore picks the big slow rank and ignores that a cheaper one on a much shorter cooldown may win on throughput.
- **src/Fiesta.Bot/Session/ZoneView.cs:2469** — The server sends the accept/turn-in script as a BURST of NC_QUEST_SCRIPT_CMD_REQ (0x4401) pages (e.g. 4 SAY pages ~100ms apart, then a DONE) WITHOUT waiting for an ack between them. The driver must ack EACH page — but a single PendingQuest field gets overwritten by the burst, so it only saw (and acked) the LAST page → "1 page answered" → QUEST_DONE never fired. Queue every page so the driver can drain + ack all of them (verified vs QuestsNew.pcapng: q11 turn-in = 5× SCRIPT_CMD_ACK).
- **src/Fiesta.Bot/Session/ZoneView.cs:2678** — ⛔ TELL THE MANAGER THE SELECTION IS GONE. TargetInvalidated was declared for exactly this and then never raised by anything — the invalidation signal the TargetAsserted doc says the "only re-target when it changes" optimisation requires. Without it the bot keeps believing it is targeted at a CORPSE: measured live on FighterFresh, `h7049 ... hp 0/? ... held 63s`, still held after travelling to another map. Handles are per-map and reused, so a retained one is not merely stale — it can name a different entity entirely, and the next bash would skip the targeting packet.
- **src/Fiesta.Bot/Session/ZoneView.cs:2829** — ⚠️ DESPITE THE ENUM NAME (NC_ACT_CANCELCASTBAR), 0x2048 fires on COMPLETION as well as on a real cancel — observed 15:10:13.792, where it landed in the SAME MILLISECOND as the RIDE_ON that proved the summon succeeded. So this line must NOT assert failure; the DURATION is what distinguishes them. A self-cancelled summon closed after 99ms; the successful one ran 2755ms (the full ~3s animation). Report the duration and let it speak — an early close means something interrupted us.
- **src/Fiesta.Bot/Session/ZoneView.cs:2954** — A measured heal ABOVE what the shop advertises means one of the two is wrong — say so rather than silently taking the larger. (Expected never to fire; if it does, the advertised value is not the whole story and the sustain model needs revisiting.)
- **src/Fiesta.Bot/Session/ZoneView.cs:3269** — ⚔️ SOMEONE ELSE'S SKILL HITTING *US*. This branch did not exist: the frame was parsed and then dropped unless WE were the caster, so every point of damage from an enemy that fights with SKILLS rather than melee swings was invisible to the attribution path. MEASURED 2026-08-13 (MageFresh, JCQ Job1_Dn01): the shadow clone landed 79-99 damage every ~2.0s — 20-24% of a 407 maxHp mage per hit — and produced 317 "[damage] took N with NO tracked attacker" lines and `aggressors=0` for the whole fight. Everything downstream was therefore starved: InCombat stayed false, the aggressor set stayed empty, and neither the per-handle damage nor the per-handle RANGE tables ever learned the one enemy they were added for. The kite could not size its loop, could not tell an elite from trash, and its own trigger (inCombat) could never become true. Routed through NoteHit so an incoming skill is bookkept EXACTLY like an incoming swing — one place, no second copy of the aggressor/threat/hp logic to drift.
- **src/Fiesta.Bot/Session/ZoneView.cs:3462** — ROOT CAUSE (operator + JCQMany diff, 2026-07-14): the server fires the room's interrupt (SkelRegen) on the ACK using the player's SERVER-side position. The bot MOVEFAIL-storms entering the area, so its server position lags OUTSIDE the trigger while the client thinks it's inside — a single ack from that desynced moment doesn't fire (the R2 intermittency + "bot does something different"; the real client never MOVEFAILs so its one ack always lands server-valid). FIX: RE-SEND the ACK every ~500ms while our (MOVEFAIL-resynced) position is INSIDE the .aid box, so an ack lands on a tick where the SERVER agrees we're inside → the server re-checks position per ack. Stop once a REGENMOB arrives (wave armed) or ~15s.
- **src/Fiesta.Bot/Session/ZoneView.cs:3474** — (1) WAIT until we've ARRIVED and parked at the trigger — no significant MOVEFAIL for ~900ms = the server has settled us at a stable, server-valid position (operator 2026-07-15: "only start the ack spam once near/inside, as confirmed by lack of movefail"). Acking while still navigating there is pointless — the server re-checks OUR server position on each ack, so an ack from outside the trigger fires nothing. WHY the old design failed the FINALE: the server sends AREAENTRY_REQ 'Zone_Mob05' while we're still EAST fighting the Chiefs (self@5293,5194, in Zone_Mob04); the old loop spammed acks from there and hit a hard 90s cutoff BEFORE we killed the Chiefs and reached Zone_Mob05 → LightOn never got an ack from inside → never fired. ARRIVED = shove-free (settled at a server-valid position) AND actually INSIDE this area's .aid box (else "shove-free" opens anywhere the bot pauses — e.g. it acked Zone_Mob02 from 1703,3140, still in Zone_Mob01, ~1300u short). The box check makes the ack fire ONLY from inside the trigger, so the finale ack for Zone_Mob05 waits until we've killed the Chiefs and walked west INTO Zone_Mob05 — not from the Chief area where the REQ first arrived. The 5-min window is plenty; no IsInsideScenarioArea data (non-scenario) → box check passes (true).
- **src/Fiesta.Bot/Session/ZoneView.cs:3554** — ⛔ ANNOUNCE THE PROMOTION. A clone is a PLAYER record that only becomes a mob by being marked here — nothing writes to _npcs, so none of the entity-mutation sites fire and a live viewer never hears about it. Measured 2026-08-13: the operator's combat map stopped drawing the JCQ clone entirely once the map was fed by the delta stream, because this was the one transition that made it drawable and it was silent.
- **src/Fiesta.Bot/Session/ZoneView.cs:3669** — LEARN THE HP-STONE COOLDOWN from the wire (never hardcode a game fact). Consecutive SUCCESSES cannot be closer together than the cooldown, so the minimum observed gap converges onto it from above. Measured 2026-08-05: 6.75s / 6.94s / 7.05s.
- **src/Fiesta.Bot/Session/ZoneView.cs:3675** — A success proves only cooldown <= gap (a CEILING). It is NOT the cooldown unless a USEFAIL just below it corroborates — see HpStoneCooldownMs. Recording it as "LEARNED cooldown" is what published 33098ms for a bot that simply heals rarely.
- **src/Fiesta.Bot/Session/ZoneView.cs:3721** — ⛔ NOT "harmless". With charges in reserve a USEFAIL means the stone is ON COOLDOWN, and the old note said the driver could just "spam again and one lands" — but the cooldown CAPS heal throughput at one per ~7s, and nothing modelled that. Measured 2026-08-05: 59 HP_USE_REQ → 23 success / 23 USEFAIL / 13 unanswered (39%). In the death at 15:30:41 ALL FIVE uses failed and HP fell 628→0 monotonically while the log claimed "soul-stone HP recharge" each time. Log it as the failed heal it is, so a bot that cannot heal is visible instead of silent.
- **src/Fiesta.Bot/Session/ZoneView.cs:3904** — ⭐ THE PER-CHARGE HEAL, STRAIGHT FROM THE SERVER. These two were parsed, printed and then DROPPED on the floor — only the maxima and prices were kept. That is the whole of the "heal cap is wrong at low level" P0 (operator 2026-08-11: JcqArcher showing 11.41 hp/s against a real ~150-per-charge; JcqMage 0.52 hp/s): the driver was inferring a sustain figure while the server had already told us the exact number. Measured live on JcqFighter: "HP restore 270 max 35 @21cen" at level 16. Authoritative, needs no measurement, and sidesteps the attribution problem entirely — a measured HP delta can be polluted by regen, a potion or an ally's heal; this cannot.
- **src/Fiesta.Bot/Session/ZoneView.cs:3992** — 🏃 THE MOVE SEGMENT, for anything that wants to draw the entity WHERE IT ACTUALLY IS. We store the DESTINATION as the entity's position (below) because that is what the bot's logic wants — but a mob that was just told to walk 240u is not there yet, it is walking, and a map drawn from the destination teleports it and then freezes for two seconds. Raw speed sits at offset 18 in the same units as ACT_MOVESPEED (see SpeedRawToUPerSec). MEASURED against a live capture (18 segments, 3 entities): segment length / time-until-the -next-move-packet = 110-122 u/s against a predicted 120.0 — i.e. the server sends the next packet just as the entity lands, so walking the segment at this speed reproduces the real motion rather than approximating it.
- **src/Fiesta.Bot/Session/ZoneView.cs:4023** — While it is STILL chasing us, every step is a fresh lower bound on how far this mob id is willing to leave home. This is where the leash is actually measured — the freeze above only sets distance 0. Grows monotonically; never guessed.
- **src/Fiesta.Bot/Session/ZoneView.cs:4032** — Only fires on a NEW maximum, so it's self-throttling: the tail shows the leash being learned, mob by mob, and stops talking once each type's limit has settled. Was `away > prev + 25` — which is NOT throttling when 40 mobs each learn their OWN maximum: measured 36 [leash] lines in 63s, and that spam then EVICTED the bag-census diagnostic from the ~200-line ring buffer (note-level window shrank to ~3 min), which is what blocked confirming the top blocker's premise. Only report a MATERIAL change: the first observation for a mob id, or a >25% jump over its previous max.
- **src/Fiesta.Bot/Session/ZoneView.cs:4059** — Note on the TRANSITION into the aggressor set, Verbose while it stays there. Every MOVERUN frame from a chaser re-stamps this, so logging it unconditionally at Note made ONE line 29% of the entire Note log (1879 of 6473 lines over a 69-min session — 6x the next-biggest category, all of it the same handful of handles at ~10/s). That drowns the headline channel used for diagnosis and burns ~a third of the 100k ring buffer's retained history. "mob X started chasing us" is a headline; "mob X is still chasing us" is per-frame perception. Same wasActive?Note:Verbose shape as the CEASE_FIRE line above.
- **src/Fiesta.Bot/Zone/ZoneEntry.cs:70** — NC_CHAR_BASE (live server: Char cmd 56 = 0x1038, the first burst frame) carries the CURRENT vitals + soul-stone reserve. Layout (verified live — CurHP@42 == MaxHp 208): CurPwrStone u16@34, CurGrdStone u16@36, CurHPStone u16@38, CurSPStone u16@40, CurHP u32@42, CurSP u32@46. This is the authoritative CURRENT soul-stone reserve at zone enter — the [1802] param block only has the MAX (its current/PwrStone fields read 0). The bot MUST seed from here, else it can't tell "reserve full" from "empty" and (a) spam-USEs at full HP (a USE at 100% HP fails), (b) tries to over-buy past the cap (buy is refused if current+buy > max). Verified: reserve was 18/23 the whole time it looked "depleted".
- **src/Fiesta.Bot/Zone/ZoneEntry.cs:279** — PLAYER_QUEST_DATA.End_NPCMobCount[5] at record offset 24 = per-objective kill counts; their sum = the quest's credited progress. The zone re-sends this authoritatively on every entry (incl. after a handover), so it's how progress survives without a persistent cache. ⭐ KEEP THE PER-OBJECTIVE ARRAY, don't only sum it. Summing here and discarding the parts is why every goal row on the watch page read 0/N while the quest header showed the true total: the aggregate was seeded on zone entry and the per-objective counters were not, so after ANY relog (and this bot relogs constantly) the only per-goal numbers came from kills credited since that login. Live 2026-08-06: "Kid Woz's Small Wish" 7/8 in the header, "kill Skeleton 0/8" underneath it. The data was on the wire the whole time. ⭐ OFFSET 25, NOT 24 — measured, not assumed. With the array read at 24 the live burst reported `48(s6,7=[0/7/0/0/0])` and `52(s8,8=[0/8/0/0/0])`: both are SINGLE-objective quests whose only objective is index 0, yet the count landed in slot 1 every time, and slot 0 was 0 for all 30 active quests. One byte early. The aggregate never showed it, because summing five bytes from one byte early still catches the real values while that leading byte is 0 — which is why this survived undetected behind a correct-looking total. Cross-check that this is the ARRAY and not the indexing: the live 0x440D credit for "Secret Hideout 2" arrived with objIdx 1 for its second objective, so the wire's objIdx does match QuestData's ordering.
- **src/Fiesta.Bot/Zone/ZoneEntry.cs:353** — Stone region of CHAR_PARAMETER_DATA: MaxHPStone @param160→body162, MaxSPStone @param164→body166 (max soul-stone reserve CAPACITY). The CURRENT counts live in NC_CHAR_BASE (0x1038, parsed above), not here. Seed the max so the bot can restock at a fraction of capacity (<10%) and compute the buy deficit.
- **scripts/auto_grind.lua:18** — Looped on the tick interval (default 250 ms). Keep it cheap and non-blocking.
- **scripts/level_quest.lua:17** — ── RANGED vs MELEE ENGAGEMENT (operator 2026-08-11) ────────────────────────────────────────────── "fix archers and mages running into melee range". They were closing to MELEE and swinging like a fighter — measured live: `[combat] melee Mandragora (Id 355) dist=54` on the MAGE, which then "LEARNED attack-range 54u" and kept doing it. The learned range is measured from where our swings CONNECT, so walking in first makes it learn a melee number: the behaviour justifies itself. WHICH weapon is ranged is DATA, not a class list: ItemInfo.WeaponType — 2 bow, 10 crossbow, 3 staff, 11 wand are ranged; 1/4/5/13/17/18/19/21 (sword/axe/mace/hammer/dual-sword/claw/blade) are melee.
- **scripts/level_quest.lua:54** — TELEPORT-JUMPS far away (~2400u) when it drops to ~80% HP, then despawns. Chasing it is futile — while it flees it STUNS us (an abstate) so movement FAILS until it despawns. So beyond this dist = "clone fled" → stop, don't chase, wait out the despawn → roomba. Well above melee drift (<~50u), well below the flee jump (~2400u). Bot-behaviour tuning, not a game fact.
- **scripts/level_quest.lua:64** — numbers and swarm-kills it; grind SAFELY below this and graduate up as we level. Pure bot-behaviour tuning (approximates the auto-aggro threshold). 💀 OBSERVED-LETHALITY THRESHOLD. A quest whose target mob has been SEEN to kill us in this many hits or fewer is demoted out of the preferred band. LEVEL IS A POOR PROXY FOR LETHALITY and this is the evidence that proves it: mob4002 was NOT flagged by SWARM_MARGIN, yet it hit for 208-246 against our 881 maxHp — FOUR hits — and killed the bot twice in 90s (-528 exp) after a FIVE-hop trip to reach it. This is bot-behaviour tuning (a risk appetite), but the number it compares against is LEARNED FROM THE WIRE (bot.mobHitsToKillUs, fed by observed SWING_DAMAGE), never hardcoded per mob. ⚠️ This DEMOTES, it does not abandon: the quest drops to the risky band and is still attempted when nothing better exists. It is target SELECTION, NOT the flee-early threshold the operator removed 2026-07-16 ("an ARBITRARY, unrealistic cap that abandoned winnable fights"). CALIBRATED TO THE MOB THAT ACTUALLY KILLED US: mob4002's 246 max vs 881 maxHp = ceil(881/246) = FOUR hits. I first set this to 3, which would not have caught the very case it was written for — set it from the evidence, not from a round number. Observed-safe mobs sit far above it (mob17=9, mob25=17, mob23=111), so 4 is not over-broad on any data we have. ⚠️ This is a LOWER BOUND on danger: it judges ONE mob in isolation, while the deaths involved 6-8 attackers stacking (480 -> 16 HP in 0.9s, ~500 dps). A mob that clears this bar can still be lethal in a pack.
- **scripts/level_quest.lua:131** — ⛔ `local TOWN = "RouN"` WAS DELETED (operator 2026-08-18: "SCRUB ALL TOWN = ROUN FROM ALL SCRIPTS. This has been superceded by automatic discovery etc. These are ALL stale"). It anchored the whole driver to one baked map name and was the direct cause of the RouN<->Eld ping-pong measured this session: a hand-in pulled the bot to Eld (where its turn-in NPC lives), then the accept path -- which asked "am I in TOWN?" and got "no" -- immediately pulled it back to RouN, six times, until `Lost Love 2 (q430)` was abandoned with its 1173 exp lost. The code had already noticed the shape twice: see the RouVal01<->RouN bounce note in handin() and the RouN<->RouCos02 one in the tour trigger. Replaced by three DISCOVERED answers, none of which name a map: isTownMap(m) -- m is a town because we DID a town thing there (markTown), not because it is RouN giverHubMap(c) -- the map that actually hosts the most acceptable givers, from MobCoordinate serviceMap(npc) -- the map a specific service NPC actually lives on, from MobCoordinate/learned ⚠️ STILL STALE, same class, NOT yet scrubbed: XPFIELD ("RouCos02") and SELL_NPC (29). TOWN DETECTION (operator 2026-07-16 "remove RouN hardcoding — gear opportunistically in whatever town the bot is in"): a map is a TOWN once the bot has done a town activity there — reached a quest turn-in NPC, or opened a shop. Learned from the bot's own activity (NO hardcoded town list). Lets the gear loop run at the Eld smith (lvl-20 armor) when the bot is there for a hand-in, not just at the hardcoded home TOWN.
- **scripts/level_quest.lua:214** — ⛔ REMEMBER THE STONE PRICE ONCE LEARNED. bot.hpStonePrice() only reports while the soul-stone menu state is current, so it flickers back to 0/nil — and 0 does not mean "free", it means NOT YET KNOWN. Every read of it went through `(price > 0) and price*10 or STONE_BUDGET_FALLBACK`, which quietly turns "unknown" into a REAL budget of 100, so the same 140 cen was judged broke (budget 200, price known) or solvent (budget 100, price unknown) from one tick to the next. MageFresh alternated between "entering UNDER-STOCKED - too broke" and "restocking to MAX before entry" 12 seconds apart, and walked RouVal01 <-> RouN for hours on the strength of it: restock=51% instance-enter=30% kill=0%, +0 exp. The price is a stable fact of the world once seen, so hold onto it. [[fiesta-nothing-yet-read-as-an-answer]]
- **scripts/level_quest.lua:241** — fight, top the HP reserve up to ~full so we never engage at 40% (instant disadvantage → the next fight starts losing). Fires ONLY right before an engage (a wanted mob in view), never continuously while idle — so a poison/DoT can't drain the reserve between fights (operator's poison caveat, satisfied by design). 🩸 TRAVEL heal threshold (2026-08-05). HEAL_FULL_PCT (95) is explicitly the PRE-ENGAGE "top to full before starting a NEW fight" threshold — using it while TRAVELLING meant any 5% of chip damage fired a soul stone every HEAL_CD (500ms) for the whole route. Measured burn: 94 -> 44 -> 10 stones in ~20 min, each exhaustion buying a town round-trip instead of questing, which starved the quest-focus path entirely (0 FOCUS lines in 110s). Mid-route we are running, not about to engage: heal for SURVIVAL, not for tidiness.
- **scripts/level_quest.lua:251** — 🩹 OUT-OF-COMBAT top-up (operator 2026-08-13: "heal up to at least 80% or 90% out of combat so they're ready for the next fight"). Distinct from all three thresholds above: this is the IDLE window between fights, which none of them covered — a bot that ended a fight at 45% simply stayed there and opened the next one at 45%. Gated on `not inCombat()`, so a DoT (which keeps inCombat true) cannot drain the reserve, which is the poison caveat the pre-engage note raised.
- **scripts/level_quest.lua:336** — "The mage should start kiting the clone WHEN IT HITS HARD" (operator 2026-08-13). Kiting every trash mob from 2s in would turn a field grind into a permanent lap of the room, so the early kite needs a test for "hard". Expressed as HITS-TO-KILL, learned from the wire per attacker: anything that can kill us in this many connects or fewer is worth running from. Measured, the JCQ clone lands ~100+ on a 407 maxHp mage (4 hits) while a RouN Prock lands 29 (14 hits), so the two sit either side of this by a wide margin.
- **scripts/level_quest.lua:342** — ⛔ THE EARLY KITE NEEDS ITS OWN ENGAGE CLOCK, NOT `engagedAt` (measured 2026-08-13: EARLY KITE fired ZERO times in a 90-minute MageFresh JCQ run, the exact fight it was written for). `engagedAt` is written only by the FIELD grind path (grindStep); the INSTANCE path keeps its own `instAtkAt` and never touches it, so inside Job1_Dn01 the gate read `engagedAt > 0` as false forever. Same shape as the gate-depends- on-what-it-gates bugs: the condition was tested against a variable the calling path does not maintain. This pair is maintained by kiteStep ITSELF, so it is true no matter who calls it.
- **scripts/level_quest.lua:415** — DIRECTED IN-COMBAT KITING (operator 2026-07-21, "Approach 2" / the ideal): when advancing to a far quest-mob spawn through a dense auto-aggro field, BOUND how many mobs are on us at once. Advance toward the goal until aggro reaches KITE_TANK_MAX, then fight it down to KITE_TANK_LOW, then advance again — capping exposure at ~MAX (survivable) instead of walking into the whole 40-mob pack (the 100%->dead burst). Pure bot-behaviour tuning (the "max tankable count" example the operator gave = 4; the char's real tankiness sets it).
- **scripts/level_quest.lua:426** — workable non-repeatable quest gives MORE than this multiple of its exp (operator 2026-07-21: "like 10x"). One-time quests are a FINITE resource (do them while available); repeatables are infinite filler — so a TRIVIAL repeatable (Ruined Garden 2 / q233 = 100 exp, ~31x dwarfed by a 3165-exp one-time quest at lvl24) must NOT outrank it ("<1% exp, terrible"). But a decent repeatable (within 10x) stays PREFERRED in band 1 — it's farmable forever. Pure exp ratio — no level curve needed. Inventory auto-sort locks the WHOLE inventory ~5s server-side (operator 2026-07-03: "inventory is fully blocked during that"). We fire the sort right after a quest reward lands (port scrolls etc. don't stack by default → sort compacts them). While locked, DON'T fire any inventory op (loot/ equip/sell/buy/hand-in) — they'd be rejected. invLockUntil gates them AND debounces the sort itself (a done quest lingering in activeQuests can't re-trigger a second sort during the lock window).
- **scripts/level_quest.lua:481** — ⭐ A STORAGE TRIP IS WORTH ITS COST BASED ON WHAT IT RECLAIMS, NOT ON WHAT IS LEFT FREE (operator 2026-08-06: "39/48 from a storage trip is only a half pass — should drop to around 15"). Both storage gates used to key ONLY on free slots (`bagFree < 6`, and `bagFull or free < 1` for travel). That measures the wrong thing: it asks "how close am I to stuck?" when the question is "how many slots would this trip actually win back?". Live 2026-08-06 the bag held 41/48 with **15 slots of pure dead weight** — six FULL stacks of HP Regeneration Potion, seven FULL stacks of Return Scroll: Roumen — and neither gate could fire (7 free, so no pressure; not bagFull, so no travel). The surplus rule was written and wired into the deposit loop, and still never ran once. Self-limiting by construction: a trip drops the surplus to ~0, so this cannot thrash the way the free-slot travel trigger did (that regression burned 82% of a 12-minute budget) — it fires, reclaims a lot, and goes quiet until duplicates build up again.
- **scripts/level_quest.lua:494** — ⭐⭐ HP POTIONS — THE BOT WAS DYING OF "HP STONES EMPTY" WITH ~23,000 HP OF HEALING IN ITS OWN BAG (found live 2026-08-06). It died on EldCem01 mid `restock: HP stones empty, go to town`, killed by Wild Imp / Ice ViVi / Bat, while carrying 61× HP Potion (Tier 1) and 12× HP Potion (Tier 2). Soul stones were the ONLY heal the driver knew, so an empty reserve meant a defenceless cross-map walk to town — and `restock` was 100% of the time budget at +0 exp. WHICH ITEMS HEAL — learned from the WIRE, not from a table, because the client genuinely cannot know: the effect table (`ItemUseEffect`: UseEffectA 0=HP, 1=SP, 2=cure, 4=abstate) is a SERVER-side Shine table, there is no `ItemUseEffect.shn` in ressystem, and `ItemInfo.BT_Inx` is 0 for every potion. The real client only ever displays a description string; the server applies the effect on NC_ITEM_USE_REQ. So we do what the client does — use one and watch HP. A candidate is Type 1 / Class 0, which is the potion group (return scrolls are Class 12, mount stones Class 22), so probing cannot teleport us; the worst misfire is drinking an Antidote or a Strength buff. Items whose use did NOT move HP are remembered as non-heals and never retried.
- **scripts/level_quest.lua:532** — ⚠️ Only conclude "not a heal" when we were NOT already at full HP — a potion drunk at 100% restores nothing and would otherwise be libelled permanently.
- **scripts/level_quest.lua:735** — ⭐ STANDOFF APPROACH (operator 2026-08-06): "you're walking TOO CLOSE to the mobs which can cause their position to thrash (they will counter move)". Both mob-approach sites walked to the mob's EXACT centre, so the bot kept walking until it physically collided — measured cast distance was `dist=1` more often than any other value, despite MELEE=45 being the intended stand-off. At contact the mob counter-moves (collision push-out), which plausibly drives the whole cluster of symptoms chased separately today: the MOVEFAIL storm (332 in 10 min), the reach learner only ever sampling 1-2u, and 0x0FCA cast rejections at dist=1-4u. So aim at a POINT AT WEAPON RANGE from the mob, on the line from us to it, instead of at the mob. STANDOFF is derived from MELEE, which is explicitly bot-behaviour tuning, not a game fact.
- **scripts/level_quest.lua:822** — TARGETABLE: does this mob have a FIELD spawn (a MobCoordinate the client uses for the quest-log marker)? A mob with no field coord only spawns in a KQ (Kingdom Quest) hourly instance, so we can NEVER reach it in the open world (it's typically a KQ instance reskin like KQ_Honeying — but we key off the MISSING COORD, not the name: operator 2026-06-30 says the KQ_ InxName prefix is NOT guaranteed). Cached: a mob's coord is static client data. (operator: an unreachable mob is OUR BUG ~95% of the time; the ONLY legit-unsolvable case is no field coord. So no-coord == phantom == skip.)
- **scripts/level_quest.lua:981** — True iff EVERY active (not-done) quest is unsolvable — or there are no active quests at all. Gates the xp-grind fallback: the bot may grind random field mobs for XP ONLY when no quest can be accepted AND all running quests are unsolvable (operator 2026-06-30). Any solvable active quest => we should quest. Returns `allUnsolvable, nActiveKnown`. ⚠️ THE SECOND RETURN MATTERS: with an EMPTY quest list this loop never runs and returns `true` by VACUOUS TRUTH — "all zero of my quests are unsolvable". That is NOT the same statement as "I have quests and none can be done", but the caller could not tell them apart, so every spawn emitted 7-9 CRUTCH[CRIT] "NO solvable quest anywhere" in a ~3s burst while the quest lists were still arriving (bot then focused a real quest ~8s later, every time). That CRIT is the top signal in the runbook's first diagnostic step, and it cried wolf at 100% of spawns — it cost two consecutive investigation passes. Same bug family as the rest of this session: A VALUE MEANING "NOTHING YET" READ AS "A REAL ANSWER" (skill id 0, passive 0, npc handle 0, missing JSON key as false).
- **scripts/level_quest.lua:1067** — ⏱️ PHASE TIME ACCOUNTING — "where does the bot's time actually go?" Needed because that question could NOT be answered from the tail: at verbose level the 200-line ring buffer holds well under a minute (measured: a 47-SECOND window), so any attempt to infer the split between grinding / travelling / town errands from the log is sampling noise. Measured exp is ~443-550 per 10 min against the operator's >=1000 expectation, and picking the right lever requires knowing which phase eats the time — not guessing that it is travel. Accumulates ms per phase and prints ONE greppable breakdown line periodically, so a long-running bot reports its own time budget instead of needing a log window nobody can capture.
- **scripts/level_quest.lua:1108** — ⛔ A PHASE CHANGE IS PROOF OF PROGRESS — clear the hard-wedge watch. The wedge detector asks "is the driver stuck?" but answers it with position+exp, and its own comment assumed "town -> pos moves". FALSE: a bot standing at a storage/healer/quest NPC moves under HARDWEDGE_MOVE and earns no exp, so ordinary town business looked exactly like a desync. Measured 2026-08-12: JcqArcher relogged 97 times (23 hard-wedges) parked at (2500,9087) while 98.5% of its hour was `restock`; FighterFresh 36 relogs while 70% was `storage`. Each relog restarts the town pass, which is stationary and exp-less, which re-fires the wedge -- a self-sustaining loop that stopped all five bots levelling. Phase transitions are the DIRECT progress signal the detector actually wanted.
- **scripts/level_quest.lua:1131** — ⚠️ TAG SHORT WINDOWS. The accumulator resets on every script re-apply, and a single trip (one sell, one restock) inside a few minutes trivially reads as ~50% of everything. That EXACT artifact produced a "sell is 47% of the bot's time" claim that had to be retracted — over a proper 22-minute run `sell` did not appear at all. Under ~15 min, say so in the line itself so nobody (me included) quotes it as a profile.
- **scripts/level_quest.lua:1180** — ⛔ FINISH THE JOURNEY YOU STARTED (operator 2026-08-13: "I see a lot of ping-ponging - Not acceptable"). Re-issuing the SAME dest was already skipped above; what was missing is the case that actually burns the day — a DIFFERENT goal re-routing us mid-journey, so two objectives on two maps take turns and neither is ever reached. Measured: FighterFresh restock wants RouN, quest-seek wants ValDn01 -> flip every ~40s, restock=68% (1152s), +0 exp MageFresh restock wants RouN, instance-enter wants RouVal01 -> restock=51% instance-enter=30%, kill=0% A half-finished trip is worth nothing: the cost is paid on arrival, so switching target mid-route throws away everything already spent. Commit to a destination and let it land; the stall crutch and the commit timeout below are what break a genuinely bad route, not a competing goal.
- **scripts/level_quest.lua:1454** — Cast cadence (operator 2026-08-04): "start at Now + Cooldown - 50ms and spam the button 5x every 50ms or so". Firing slightly early puts the request in flight as the server's cooldown expires, so the round trip is hidden instead of paid after every cooldown; the repeats cover a press that was still marginally too early. A cast rejected on cooldown is harmless — the real client spams too.
- **scripts/level_quest.lua:1557** — ── MOUNT support (operator 2026-07-17, decoded from RepeatableQuestingLvl23…pcapng) ────────────────────────── A MOUNT is ItemInfo Type1/Class23 (e.g. "Hobby" id38033, DemandLv20 → bought lvl-20 from Kenton); its FOOD (Floatation Stone id2522) is Type1/Class22. Mount/unmount = bot.useItem(mountSlot) TOGGLE (both send NC_ITEM_USE_REQ 0x3015 — no new C#); bot.mounted() reads state; town-portal auto-dismounts. Mount ≈50% faster → transit through/past mob groups (outrun the pile-on), unmount before combat. These are ItemInfo class-enum values (like ENCHANT_KEEP_CLASS / ARMOR_SLOTS above), derived from the capture — not baked item ids.
- **scripts/level_quest.lua:1608** — Mount attempts are budgeted PER ROUTE, not polled. The summon roots us for ~3s (see mountUp()), so every attempt costs one cancelled walk. Paying that ONCE at the start of a multi-hop route to gain ~50% speed for the rest of it is clearly worth it; paying it repeatedly mid-route is what shredded travel before. Reset on the rising edge of bot.traveling(). MOUNT_TOGGLE_CD already spaces retries 5s apart.
- **scripts/level_quest.lua:1643** — ⛔ NEVER mount while the travel driver is taking a GATE HOP: a gate is SILENTLY ignored while mounted (proven 2026-08-04 — mounted gate-use aborts after 14s, unmounted transitions in ~150ms). C# sets this flag around the gate click; without it our tick mounts mid-approach and the hop dies.
- **scripts/level_quest.lua:1647** — ⛔ NEVER SUMMON THE MOUNT WHILE TAKING DAMAGE — it is refused, and the refused USE still CANCELS OUR MOVEMENT. Measured over one 69-min session (2026-08-05): 58 MOUNT-up attempts → being hit in the prior 3s : 0/21 RIDE_ON (1/21 even at a 10s window) not being hit : 22/37 RIDE_ON and 53/58 attempts were followed by a MOVEFAIL within 1.5s, 51/58 by `walk-path aborted`. The server snaps us back ~130ms after the USE, which kills the walk we were in the middle of. ALL THREE deaths that session are that exact sequence (e.g. 14:01:47.545 use → .677 MOVEFAIL + walk-path aborted → .003 DEATH): the SHED mounted "to escape faster", the summon was refused, the escape walk died with it, and the bot stood still while 9-15 mobs beat it down. -264 exp each. The doctrine comment above already said "taking damage blocks re-summon" — this enforces it. NOTE: this gates SUMMONING only. Staying mounted while chased is still fine and still wanted (that is the whole "ride THROUGH mob groups" strategy); dismount-on-engage is handled separately.
- **scripts/level_quest.lua:1660** — ⚠️ The summon ROOTS us: the server halts us ~50ms after the USE → MOVEFAIL → `walk-path aborted`, measured 91% (not hit) / 100% (being hit). So EVERY attempt costs one cancelled walk — it is never free while moving. A `if moving() then return end` guard here was tried 2026-08-05 and went too far: during travel the bot is essentially ALWAYS moving, so it never mounted at all (measured: 0 MOUNT-up / 0 RIDE_ON over 2 routes and 5 gate hops in 10.4 min) — travel ran ~50% slower on foot. The cost is real but it is a ONE-OFF, so it is budgeted per route at the call site (routeMountTries) rather than suppressed here.
- **scripts/level_quest.lua:1771** — ⛔ CAN WE ACTUALLY PAY FOR IT? The driver used to pick what it WANTED and never once compared the price to the purse, so a too-expensive item was requested, rejected by the server with BUY_ACK 0x020B, and requested again — forever, in town, instead of going out and earning the money. Measured live 2026-08-11: JcqFighter held 131 cen and kept buying One Handed Sword Mastery [02] at 290; JcqCleric held 95 and kept buying Bronze Necklace of Blessing at 380. The server was RIGHT both times; the rejection is not a bug, ignoring it is. Logged once per (item, purse) so it re-reports if our money changed — money going UP is exactly when the answer flips, and a silent skip would hide a permanently-unreachable item.
- **scripts/level_quest.lua:1908** — The distance we should ENGAGE from. Melee weapons close to MELEE as before. A ranged weapon stands off at the reach we have actually MEASURED on the wire (learnedRange), backing off ~15% so a step of drift doesn't push us out of range mid-fight. Until a ranged hit has ever landed, learnedRange is still a melee number, so we deliberately do NOT trust it below MELEE*2 — we use the shortest of our damage skills' ranges instead, which is real client data (ActiveSkill.Range) and always ranged for a bow/staff. Falls back to MELEE when we have neither, which is the level-1 case the operator called out as fine: "At level 1 it's okay, we don't have enough skills yet."
- **scripts/level_quest.lua:1942** — The learned STUN skill (ActiveSkill abstate = a stun, e.g. Concussive Charge id60 = StaBattleBlowStun 100%), if off cooldown. Cast on the target when we KITE to heal on low HP so the FROZEN mob can't chase/hit while we create space + heal (operator 2026-07-16 "stun and kite on low hp"). Data-driven via skillInfo.stun — NO baked id. Is this zero-damage, abstate-applying skill (a BUFF) worth casting right now? Only if the state it applies is not ALREADY on us (operator 2026-08-12). A buff with no abstate at all is not something we can reason about, so we leave it out of the rotation rather than spam it blind — if one turns up that matters, decode what it does and give it a gate of its own. ⚠️ Buffs land on OURSELVES, so this reads bot.selfAbstates(). When party buffing arrives, the same question gets asked of the ALLY's abstates — the shape of the check does not change.
- **scripts/level_quest.lua:1965** — ⛔ "I HAVE NEVER SEEN IT" IS NOT "IT IS NOT THERE" (measured 2026-08-12). The abstate test above is the right rule and it worked as designed — it just never gets a chance to say no. ClericFresh's Protect [01] applies StaBlessingMain (wire abstate 18) and Resist [01] applies StaMentalShoutMain (26); the self-abstate channel has NEVER reported either. It reports 290/291 (a stun) and 729/730 (StaE_UserNewbie02/03), so the channel works — these two simply do not arrive. So "missing" is vacuously true forever and the buff is re-cast on every cooldown: 95 of the cleric's 107 casts in one 18-minute capture were these two, at 30 and 46 SP each, which is exactly the SP-stone drain the operator watched. Until the decode gap is closed, DO NOT let an unverifiable buff loop. A real player casts it and moves on; so do we. This backoff is bot behaviour, not a claim about the buff's duration — the honest fix is to find the packet that carries these states (P1) and delete this branch.
- **scripts/level_quest.lua:2066** — ⛔ ONE cast per pass, and NEVER while a cast animation is still running (operator 2026-08-04: "only cast when not on cooldown AND not in cast animation already"). This loop used to fire EVERY off-cooldown skill in a single pass. On engage all of them are ready, so it sent FIVE casts in EIGHTEEN MILLISECONDS — and since each cast routes through FaceAndStop, each one's NC_ACT_STOP_REQ cancelled the melee BASHSTART swing stream (server answered CEASE_FIRE). Measured cost: the auto-attack lived ~50ms, and in the 36s before a death the bot dealt damage ONCE while taking it 77 times. The real client (Z:/CombatExtensive.pcapng) casts ~8 times in a whole capture and otherwise lets ~35 swings stream uninterrupted. ⭐ The SERVER is the source of truth for cast state (operator 2026-08-04). bot.casting() is driven by the server's own CAST_SUC_ACK / HIT_DAMAGE / CAST_FAIL / CASTABORT / CASTCUT (sequence read off Z:/CombatExtensive.pcapng). It is opened SPECULATIVELY the moment we send a cast — so we still fire optimistically and don't stall on a round trip under lag.
- **scripts/level_quest.lua:2109** — Rank the candidates ONCE, then walk them: a skill that is on cooldown, unaffordable, or inside its own re-press gap is SKIPPED, never a reason to abandon the pass. That single `return` is what let one skill monopolise the rotation. ⭐ RANK BY MEASURED THROUGHPUT, AND EXPLORE WHAT HAS NEVER BEEN CAST (operator 2026-08-13: "a 1.8s cooldown applied to all is BETTER than a 4s. IF the (actual, final) damage is > ~50%, then lvl 1 is BETTER. Shared cd just means if you cast any, the resulting cooldown applies to ALL LEVELS."). Ranking by the client's `damage` column is ranking by the WRONG NUMBER: it is a contribution over a shared weapon/magic base, so it exaggerates the gap between ranks. MEASURED: 6003 carries damage 176 and lands ~104-130; 6002 carries 112 and lands ~110 — 57% apart in the table, under 15% on the wire. Ranking by the table therefore always picks the big slow rank, which is exactly why Magic Missile [01] (1.8s) has never once been cast against [04] (4s) and the question of which is better stayed open. dps = measured final damage / cooldown. A skill with NO samples is UNKNOWN, not weak: it sorts first so it gets tried a few times and measured. That exploration is bounded — once it has samples it is ranked on its merits like everything else.
- **scripts/level_quest.lua:2132** — ⛔ DROP DOMINATED RANKS BEFORE ANYTHING ELSE (operator 2026-08-13: "There is no point in ever trying any skill of a lower level with the same cd — only if it has shorter cd"). Within one skill FAMILY the ranks share a cooldown clock, so a lower rank on the SAME cooldown is strictly worse in every situation: less damage, no sooner. It can never be the right press, and exploring it to "measure" it is measuring something we would never use. Only a rank with a genuinely SHORTER cooldown earns a place, because that is the one thing a weaker hit can buy — more presses. Concretely for the mage: Magic Missile [01] is 1800ms against [02]/[03]/[04] at 4000ms, so [01] and [04] both survive and the middle ranks are dropped outright. [02] had been quietly taking casts.
- **scripts/level_quest.lua:2176** — READY 50ms EARLY so the request is already in flight when the server's cooldown expires.
- **scripts/level_quest.lua:2223** — Only bank the cooldown once the SERVER confirms the cast actually started; otherwise leave the spam window open so the next press (50ms later) can land. bot.castConfirmed() is set by CAST_SUC_ACK.
- **scripts/level_quest.lua:2230** — ═══ SHARED COMBAT CORE ═══════════════════════════════════════════════════════════════════════════ ⛔ THIS EXISTS BECAUSE THE FIELD AND INSTANCE COMBAT PATHS KEPT DRIFTING APART. Operator 2026-08-12: "this is like the 4th time it bit that an improvement in 1 didn't make it into the other." The audited count is FIVE, and every one was a real regression living in the field path only: 1. ranged engage distance (2026-08-11) — the instance closed a mage to melee against its own ranged clone; found 2026-08-12. 2. the bot.bashing() re-bash (2026-08-05) — the instance issued auto-attack on a target CHANGE only, so once the swing stream lapsed mid-fight nothing ever restarted it. 3. the low-HP kite (2026-07-01) — absent from the instance entirely. Measured: MageFresh stood at exactly 113u for three whole clone fights while its HP fell 330 → 2. 4. safest-heading kite choice (2026-08-12) — retreating into a pack instead of away from it. 5. stun-on-engage (2026-07-16) — field only. ⭐ THE RULE, AND IT IS THE POINT OF THIS SECTION: Anything true of FIGHTING lives here, and BOTH callers use it. Only what is true of WHERE WE ARE STANDING stays in the caller — how we close the distance (the both walk themselves — nothing on the server closes distance for us — but the instance closes only to engage range and never zigzags, whether we may drag a mob out of a pack, whether restocking is possible. If you are about to write a combat improvement inside grindStep or inside the instance branch, it almost certainly belongs in one of these three functions instead.
- **scripts/level_quest.lua:2268** — ⛔ ISSUE ON A TARGET CHANGE ONLY. THE RE-BASH IS THE HOST'S JOB, NOT OURS. The first version of this function also re-issued whenever `not bot.bashing()` for 1000ms, which read like the field path's old behaviour and looked harmless. It is not: BotManager already re-bashes from the CEASE_FIRE signal, and deliberately WAITS 1200ms first to see whether the stream resumes on its own (it does, for a real player, 46% of the time) precisely because re-bashing on every cease-fire thrashes. Two owners of the same decision means both fire. ⭐ MEASURED COST OF GETTING THIS WRONG, on ClericFresh: BASHSTART sends went from 374 in 18.5 minutes to 1854 in 27.8 — 20/min to 67/min — while landed swings FELL from 92 to 71 and swings-per-bash collapsed from 0.25 to 0.04. Every extra send restarts the 418ms windup from zero, so bashing harder produced strictly less damage. The host owns "the stream died, restart it" because it is the only side that sees CEASE_FIRE.
- **scripts/level_quest.lua:2352** — ⛔ JOIN AT THE NEAREST WAYPOINT, THEN RUN THE WAY THAT LEAVES THE ENEMY (fixed 2026-08-13). Entering "at the point furthest from the enemy" was measured doing the opposite of its intent: it sent the bot on a 1421u march to the far side of the room — straight ACROSS the middle, past the chaser — and because the loop is re-fitted on every re-entry it then marched back (wp 1/50 at 1421u, then wp 29/50 at 1281u). Joining at the nearest point makes the hand-off cheap; the DIRECTION is what makes the first leg run away, and a lap is away from the enemy for half of it regardless.
- **scripts/level_quest.lua:2438** — CHIP the mob with DAMAGE skills while we kite (operator 2026-07-07): if we retreat and cast nothing, the mob leashes + regens to full and the fight never completes (seen live: mob hp 33% -> 100% during a kite). ⚠️ ONLY chip once we have CREATED DISTANCE. Casting ROOTS the character, so chipping while the mob is still ADJACENT pins us standing in melee taking hits — the kite never makes space and we die (live 2026-07-17: hp 36% -> 15% -> dead DURING a kite, chip whiffing, mob at melee throughout). In melee → PURE RETREAT first; chip only from range, where the bleeds still land and the mob is not hitting us. That resolves chip-vs-escape BY DISTANCE and keeps both operator mechanics. ❄️ TODO (operator 2026-08-13: "ice bolt can slow which can make kiting easier"). Not wired, because "does this skill slow" is not derivable from the client data we have: AbState.shn carries no movement-speed field, and the effect lives in SubAbState's ActionIndexA..D, an enum that is not in the PDB extract. The options are to decode that enum or to hardcode the ice-bolt id with the operator's permission; shipping a slowSkill() that silently matches nothing would be worse than not having one, since it would look wired and never fire.
- **scripts/level_quest.lua:2541** — ⛔ A RANGED CHARACTER STARTS KITING IMMEDIATELY, NOT AT LOW HP (operator 2026-08-13: "I think the bot starts doing it too late, should do it within like 2 seconds of seeing the enemy (~2 hits)"). Waiting for HEAL_PCT means taking the whole beating first and only then trying to recover from it — for a class that fights at 400u there is no reason to stand still for ANY of it. Measured, the mage died in ~4s per attempt at the clone, so a trigger that waits for low HP never fires in time at all. Melee is unchanged: it must close to swing, so retreat-and-heal remains its rule. ⛔ THE CLOCK IS kiteSeenAt, NOT engagedAt. engagedAt is written only by the field grind path; the instance path keeps its own instAtkAt, so gating on it made this branch unreachable inside Job1_Dn01 — MEASURED: zero EARLY KITE lines in a 90-minute JCQ run, while the low-HP kite below fired dozens of times from the same call site.
- **scripts/level_quest.lua:2583** — ⛔ STOP SPAMMING WHEN THE SERVER SAYS "NOT READY" (wire-proven 2026-08-05, RouVal02 death). The spam window exists so a press rejected for being a few ms EARLY still lands. A press rejected with CAST_NOT_READY (0x0FC8) is NOT early — the skill is genuinely on cooldown (Bone Slicer [02], 15s cd, cast at 07:55:44.42 and still refused at :50.4), so re-pressing cannot help. Banking the cooldown here ends a window that had nothing left to win. ⚠️ THE EXACT MECHANIC (operator, 2026-08-05 — I had this wrong twice, so state it precisely): A skill cast does NOT cancel an auto-attack that is already SWINGING. It DOES cancel the auto-attack's WALK-OVER: auto-attacking a distant enemy makes the client walk to it, and spamming an on-cooldown skill during that walk means when the walk finishes, NOTHING HAPPENS. That is precisely the death this was pinned from — the bot was never in melee, it was chasing (dist 54 -> 38 -> 171 -> 65 across a 16s fight) while re-pressing an on-cooldown Bone Slicer [02]. Every press cancelled the approach, so it never arrived, never swung, and dealt damage exactly ONCE (the cast that DID land at 07:55:44.42 — 0x2435 + three 0x2452 HIT_DAMAGE) while dying 55% -> 0%. Ending the spam window on NOT_READY lets the walk-over complete and the swings start.
- **scripts/level_quest.lua:2677** — ⛔ NEVER SWING AT SOMETHING WE CANNOT KILL (operator 2026-08-05, the Marlone loop). This picked the NEAREST aggressor, full stop. So when the Marlone BOSS walked up it became the nearest, and the bot auto-attacked a 9,916 HP GradeType-1 elite — at a measured 32-36 damage per swing that is ~290 swings — while it and its adds burned our 881 HP in seconds. Operator watched exactly that: *"ran straight into it … hit Marlone like once, instantly died. Over and over. Tunnel vision just on this mob in perpetuity."* questTargetsBoss already screens boss KILL-TARGETS at quest selection, but nothing screened what we hit BACK — and the boss aggros us simply for walking through its field to reach the (perfectly killable, 988 HP, grade-0) quest mobs. Data-driven via MobInfo GradeType, no baked ids.
- **scripts/level_quest.lua:2703** — ONE BASH PER ENGAGEMENT (operator 2026-08-12). This used to also re-issue every RETAL_REFRESH_MS, which is the same automatic re-bash removed everywhere else: each re-issue restarts the 418ms windup from zero, so refreshing produced less damage, not more.
- **scripts/level_quest.lua:2718** — 🏃 ARRIVAL SHED — outrun the tail using each chaser's OWN measured leash (operator strategy B, 2026-08-05). WHY THIS AND NOT "fight the pack": measured `[dmgtaken]` on RouCos03/RouVal02 shows a single field mob hits for **11-18 vs our 881 maxHp** (~55 hits to kill). One mob is harmless; deaths are a VOLUME problem — the accumulated tail the operator described from the start ("a huge tail of random enemies ... lets them kill it"). Culling a spawn field is unwinnable (kill one, another aggros — measured: tail stayed 2/4 across 3000u). So SHED, don't cull. HOW, entirely from wire-learned data (no baked leash): each aggressor carries fromSpawn / chaseLimit / willDropIn (bot.aggressorSpawns(); limits learned from RUN steps, verified sane e.g. 244u). A chaser with willDropIn > 0 has NOT yet hit its own limit and will keep coming; one at <= 0 is past everything we have ever seen its type do and should drop. So: while any chaser is still "leashed in", keep moving AWAY from the pack centroid; once none are, stop and let normal combat kill the few that stuck (each is harmless). Chasers with an UNMEASURED limit (chaseLimit == 0) are treated as still-chasing: safer to keep running.
- **scripts/level_quest.lua:2778** — mounted = ~50% faster = shed sooner, so ask — but mountUp() now REFUSES while we're being hit, because summoning mid-shed never worked (0/21) and cancelled the escape walk that kept us alive. This still fires once we've actually broken away (no damage for ~8s) and speeds the run home.
- **scripts/level_quest.lua:2881** — after, DON'T "approach" a far clone — that's the R1 clone that fled at ~80% and despawns in ~5s, NOT a STANDING R2/R3 clone. (The R2/R3 clone never aggros, so lastCloneFight stays old → approach fires on sight.)
- **scripts/level_quest.lua:3146** — Count deaths in the KNOWLEDGE STORE (PVC), not a script-local: locals die on every re-apply and this bot respawns constantly, so a local count never lived long enough to rank anything. Pass the LEVEL so the threshold counts deaths AT THIS LEVEL, not lifetime. With a lifetime count a quest that killed us twice ever was re-deprioritized on every later death forever (q2564 had 30), so the board went dark and needed a self-heal pass — a crutch on top of a gate that should not have fired. Operator 2026-08-06: "they should not be deprioritised in the first place". ⛔ ONLY BLAME THE QUEST IF THE QUEST'S OWN MOB KILLED US (operator 2026-08-06: "Quest deprio due to overpulling while travelling to quest" — confirmed by the death report, not inferred). Both deaths measured at 11:26 and 11:29 were `doing=KITING+MOVING` with `target=NONE`, killed by a TRAIN of aggro that was entirely `quests[none]` (4x Marlone Captain, Clan Soldier, Silver Slime) while merely travelling to the focus quest's mob. Charging those to the focus quest is a factual error, and it is what fills the board with lvl-N marks that only a level-up can clear — the ratchet. So: check whether ANY aggressor is one of this quest's objective mobs. If none is, the quest did not kill us; a pull did. Record nothing against the quest and say so. This is an ATTRIBUTION fix, not an expiry/self-heal crutch (that was tried and rightly rejected).
- **scripts/level_quest.lua:3200** — ⛔ A DEATH WITH NO FOCUSED QUEST WAS COMPLETELY SILENT — no log, no learning, just respawn. Measured over a 75-min window: only 2 of 6 deaths logged anything. The other FOUR (a cluster of 4 deaths in 13 minutes on the same mobs) fell into this branch, so nothing was deprioritized, nothing was recorded, and the bot walked back and died again. The last FOCUS line was 50 minutes earlier — those deaths happened during travel/gear/storage phase cycling, where the quest deprioritization mechanism cannot reach by construction. Deaths are the #1 exp sink (they consumed 102% of everything earned in that window), so a death that produces NO record is the most expensive possible silence. Report it with the evidence needed to act: where, doing what, who killed us, and whether the fight was arithmetically winnable (incoming DPS vs what our stone can sustain).
- **scripts/level_quest.lua:3266** — ⛔ DON'T dismount just because something hit us (operator 2026-08-05: "bot dismounts when detecting an enemy is chasing it — if target far away it should just keep running on mount"). bot.inCombat() means "took damage in the last ~8s". The mount exists precisely to RIDE THROUGH mob fields (see the comment above), and riding through mob fields means taking hits — so the old `if inCombat() then dismount()` defeated its own purpose: one hit dropped us off, the re-mount guard below then refused to re-mount for 8s, the next hit re-armed it, and the bot WALKED the whole route. Mounted we are ~50% faster, so a chaser that is not already on top of us simply gets outrun. Dismount ONLY when an aggressor is close enough to actually engage us. ⛔ ...AND WHILE TRAVELLING, DON'T DISMOUNT FOR AGGRO AT ALL (2026-08-05, follow-through from removing the mid-travel retaliation). Dismounting is only worth it if we are going to FIGHT — and we no longer fight while a route is walking us, because the walk cancels the swing stream (see the KITE-ADVANCE and mid-transit comments). So dismounting mid-route now buys literally nothing: we get ~50% slower AND stay in the pack longer, which is exactly what drains the soul stones. Measured cost of the old behaviour: hpStones 94 -> 40 -> 0 in ~20 min, ending in `restock: HP stones empty -> RouN` with kills=0 — the bot spent the session ferrying itself to town instead of questing. Mounted we outrun the tail; the arrival shed (P0) is what clears whatever is still attached.
- **scripts/level_quest.lua:3291** — NOTE: no timed feed — USE'ing the Floatation Stone on a non-hungry mount returns err 0x10C9 (gauge full), so a 25s timer just spams a failed action. Need-based feeding waits on the fed-gauge decode (P2). Symmetric with the dismount rule above: a distant chaser must NOT block re-mounting either, or one hit strands us on foot for the whole 8s inCombat window (and the next hit re-arms it → we walk the entire route). Only a mob genuinely ON us should keep us dismounted. 🐴 MOUNT ONCE PER ROUTE — budgeted, not polled. Summoning ROOTS you: the server halts us ~50ms after the USE, which nav reads as MOVEFAIL → `walk-path aborted`. Measured over a 69-min session, 58 attempts: being hit : 21/21 MOVEFAIL (100%), 19 walk-aborts, median 42ms after the USE NOT hit : 34/37 MOVEFAIL (91%), 32 walk-aborts, median 57ms after the USE So this is NOT a combat effect and NOT a refusal side-effect — the summon always stops you, and every attempt costs a cancelled walk. Two wrong fixes were tried before this one: 1. `and moving()` — mounted ONLY while walking, i.e. exactly when it was guaranteed to shred the walk. 2. `and not moving()` — during travel we are essentially always moving, so it never mounted AT ALL (measured: 0 MOUNT-up / 0 RIDE_ON over 2 routes + 5 gate hops in 10.4 min) and every route ran on foot at ~50% speed. It also made the pre-gate dismount fix unverifiable (never mounted ⇒ never dismounted), which is its own kind of harm: a change that hides the evidence for the next one. The right frame is that the interruption is a ONE-OFF cost against a whole-route benefit. Pay it at the start of the route (walk cancelled once, travel re-paths in ~1s), then ride the remaining hops ~50% faster. ROUTE_MOUNT_TRIES bounds retries so a refused/dropped summon cannot turn into mid-route thrash.
- **scripts/level_quest.lua:3311** — 🔍 LOG WHY WE DIDN'T MOUNT. Six separate guards can veto a transit mount (route budget, aggro-in-reach, stone-starve, noMount/gate-hop, inCombat, toggle cooldown) and NONE of them said so, which cost a whole pass to "0 MOUNT-up over 2 routes — but which guard?" with no way to answer except guessing. The bot is observed only through /log, so a decision this contested has to state its reason. Throttled to one line per reason-change per route so it cannot become the next 29%-of-the-log offender.
- **scripts/level_quest.lua:3369** — 🩹 TOP UP BETWEEN FIGHTS (operator 2026-08-13: "we should have bots use hp stone to heal up to at least 80% or 90% out of combat so they're ready for the next fight"). The three existing thresholds all miss this window: HEAL_PCT (40) is mid-fight survival, TRAVEL_HEAL_PCT (55) is mid-route survival, and HEAL_FULL_PCT (95) fires ONLY in the instant before an engage. So a bot that finished a fight at 45% just stood there at 45% and started the next one already losing — the exact "instant disadvantage" the pre-engage heal exists to prevent, except the pre-engage path only fires when a wanted mob is already in view. ⛔ GATED ON `not inCombat()`, which also answers the poison caveat the pre-engage note worried about: inCombat() means "took damage in the last ~8s", so a DoT ticking on us keeps it TRUE and this cannot drain the reserve chasing damage that is still arriving. It only fires when we are genuinely idle.
- **scripts/level_quest.lua:3382** — ⛔ NOT dbg(): that is throttled to ONE line per 2s across every caller, so this action logged nothing at all while it was demonstrably working (JcqArcher 65% -> 100%, one stone spent). An action the bot takes has to be visible or it cannot be debugged. Own throttle, Info level.
- **scripts/level_quest.lua:3406** — Scenario-instance context, computed EARLY — needed BOTH by the flee gate just below AND the restock/sell/ gear phases further down (all town-trips are suppressed inside the instance). Inside the bounded promotion instance the mobs ARE the objective and there is NOWHERE to flee, so the generic OVERWHELMED full-flee must be SUPPRESSED here: with it active the bot stands frozen against a wall (move=false) tanking the whole skeleton wave, soul-stone-healed but NEVER fighting → R2 never clears (root cause found 2026-07-14 from the live trace: `FLEEING — aggressors=3 ... view[4]:{mob3009x3,mob3008x1}`, hp yo-yoing 30%↔100%, 0 kills). instanceMap is learned while a scenario area is active, then "still on that map" counts as in-instance even when scenarioArea() momentarily flips "" between rooms. Data-driven (learned), no hardcoded map name.
- **scripts/level_quest.lua:3436** — HARD-WEDGE watchdog (2026-07-29, tickets.md P0): if BOTH position AND exp are frozen for HARDWEDGE_MS, the bot is truly stuck — a client↔server position DESYNC where the client thinks it's somewhere the server doesn't, so every move is server-rejected (MOVEFAIL-pin). Observed live: JcqFresh 10 min frozen at RouVal02(8726,8363) while the server had it on EldGbl02; nothing self-recovered. A clean RELOG re-reads the server's authoritative map+pos and un-wedges (proven fix). SAFE gating: requires BOTH pos-frozen AND exp-frozen (town → pos moves; combat → exp climbs; transit → pos moves), NOT in an instance (they have their own relog + relog reloads the run), and a cooldown so a re-wedge can't hot-loop. This is a RECOVERY safety net; the ROOT (prevent the desync from a chase across a map boundary / handle the missed LINKSAME) stays open in tickets.md P0. Phases whose WHOLE JOB is standing still at an NPC. Position+exp cannot distinguish these from a wedge, so the detector must not run in them at all (a stall INSIDE one is a real bug to fix in that phase -- relogging only restarts it from the top and hides it). ⚠️ ONLY phases that stand at an NPC for their WHOLE duration. `restock` is deliberately NOT here: it includes the cross-map TRIP to town, and a genuine nav wedge during that trip is exactly what the detector must still catch. Measured 2026-08-12: JcqArcher sat at (2500,9087) on Linkfield02 with every move server-rejected ("move blocked — resynced") trying to use gate h=17278 — a REAL desync, correctly relogged. Excluding `restock` would have left it wedged forever.
- **scripts/level_quest.lua:3518** — OVERWHELMED auto-full-flee DISABLED (operator 2026-07-16): the "aggressors >= N (=3) & hp<40% → FULL FLEE + deprioritize the quest" bail was an ARBITRARY, unrealistic cap that abandoned winnable fights and drove the flee→deprioritize→LAST-RESORT crawl. Now that BOSS/elite kill-targets are deprioritized up front (questTargetsBoss, GradeType>=1), the remaining quest mobs are meant to be doable — so let the bot run its NORMAL combat routine (auto-attack + skills + stone-heal) until it WINS or DIES. Death is cheap (revive restores HP, kill-credit survives), and dying-while-fighting is the signal that combat must get SMARTER (proper ranged single-pull + stun[Concussive Charge id60]/kite on low HP — P1), NOT a reason to flee early. The explicit `if fleeing` handler above still serves safe-logout / scripted flees; only the auto-trigger is gone.
- **scripts/level_quest.lua:3527** — ⚔️ FIGHT YOUR WAY THROUGH WHEN AGGRO'D MID-PATH (operator 2026-07-21: "fight your way through the crowd when pathing somewhere and you get aggro'd — ONLY then; if the mobs are peaceful by default don't bother them"). ROOT of the Burning Hill 87%→dead burst: while APPROACHING/travelling the bot kept WALKING through a pack that aggro'd it, so 40 mobs piled on and burst it before grindStep (which only targets the QUEST mob) ever fought them. Fix: the instant something is ATTACKING us, STOP pathing and fight the AGGRESSORS down — their types only, so peaceful mobs we're merely walking past are left alone (operator "don't bother them"). Killing a Marlone aggressor (mob20/21) also credits q55, and mob313 credits q452 → survival AND progress. Travel resumes automatically once clear (aggressors==0). GATES: skip while fleeing (low-HP emergency owns the tick), inside a scenario instance (its wave combat is separate), or when stone-starved (can't out-heal a fight with no stones → let restock/flee run; the potion-heal P1 will later cover this window). This is the normal "cut through what's hitting me" response the operator wants — NOT flee-early, NOT walk-through-and-burst.
- **scripts/level_quest.lua:3579** — ⛔ DO NOT RETALIATE WHILE ADVANCING (measured 2026-08-05, reverting my own change from earlier today). retaliateInPlace() itself issues no walkTo, but THIS BRANCH walks one line below — and the walk cancels the melee swing stream. The wire showed the result plainly: bash sessions 355->368 in ~3s, every RE-BASH killed by a CEASE_FIRE 30-55ms later, zero sustained damage: 00:12:08.459 KITE-ADVANCE: 3/4 on us - walking to goal 00:12:08.966 RETALIATE in place: mob313 h=870 at 47u 00:12:08.989 CEASE_FIRE (session 367) / RE-BASH h=870 00:12:09.043 CEASE_FIRE (session 368) So attacking while moving is not "free damage", it's thrash. Retaliation belongs where we are STATIONARY (the arrival case). While advancing, keep advancing — the shed is what clears the tail.
- **scripts/level_quest.lua:3635** — ⚔️ FIGHT THE TAIL WHILE WE RUN (operator 2026-08-05). The no-grind rule above is about not issuing a competing CHASE walkTo — it was never meant to make us unable to defend ourselves. retaliateInPlace() only ever swings at something already inside our own reach and issues NO movement, so the travel route survives intact while the escorts that are actually biting us die (and pay exp) instead of accumulating into the pack that kills us on arrival. Same measured reason as the KITE-ADVANCE branch above: an autonomous travel route is walking us, and walking cancels the swing stream, so swinging here only thrashes bash/CEASE_FIRE. Run the corridor; the arrival shed is what clears the tail.
- **scripts/level_quest.lua:3748** — Restock HP stones when the reserve is EMPTY (USEFAIL) or known to be below 10% of capacity — top up early instead of getting stranded mid-field at 0. The count is unknown (-1) until the first buy/use, so depletion is the fallback for the very first reserve. Skip while a recent restock keeps failing (no money?). TODO P3: flee + graceful combat-exit before going. Only restock if we can AFFORD it. When BROKE, restocking is pointless — don't loop to town for stones we can't buy; GRIND instead (a tanky-enough fighter auto-attacks fine, HP regens between fights, and death is cheap — revive restores HP and quest kill-credit survives). As selling/quest rewards build money up past the budget, restock kicks back in. (Money is seeded at login now.) Budget SCALES with the stone price (operator: ~10× current stone cost) — 300 flat is way too high at lvl 3 (stones ~7 each) and too low past lvl 20. Falls back to a low floor until we've learned the price from a shop-open (so a fresh char restocks once → learns the price → uses 10× thereafter). Compute the scenario-instance context EARLY (before restock/sell/gear/etc.) so those town-trip phases are SUPPRESSED while inside the promotion instance — you can't reach a shop/healer/smith in the instance, so restock() there just FREEZES the bot (it went PHASE=>restock and stalled forever mid clone-fight). Learn the instance map while a scenario area is active, then treat "still on that map" as in-instance even when scenarioArea() momentarily flips "" between rooms. Data-driven (learned), no hardcoded map name. (scenQ / instanceMap / inInstance were computed EARLY, above the flee gate — reused here.)
- **scripts/level_quest.lua:3798** — POST-DEATH RESTOCK (2026-07-17): breaks the death loop on tough mobs (q419 Gold Slime L24 — the bot revived with ~11/61 stones, re-engaged the same L24 mobs stone-starved, died again ~4x/10min, exp frozen). stonesLow() only trips at <10% (~6), so it fights under-resourced. For ~30s after a death, restock if the reserve is below HALF — resupply before re-engaging (what a real player does), so the next fight has a full reserve to heal through. Not a give-up: it goes right back to the same quest, just topped up.
- **scripts/level_quest.lua:3805** — ⛔ FULL STONES BEFORE AN INSTANCE — BOTH KINDS (operator P0 2026-08-12: "make sure HP and SP stones are bought TO MAX before attempting JCQ"). There is no restocking inside, and the clone fight is a sustain race — measured, the mage loses it on sustain rather than damage — so entering on a partial reserve loses the run before it starts. stonesLow() only trips near EMPTY (<10%), and "not low" is not "full", so the normal trigger is the wrong threshold for this one decision. Maxima come from the wire ([1802]); a max of 0 means we cannot judge fullness, and then we let the run proceed rather than block it forever.
- **scripts/level_quest.lua:3945** — ⛔ LABEL THE PHASE ONLY IF THE TRIP ACTUALLY ACTS (operator 2026-08-13: ClericFresh "looping on 'Doing quest X' and 'Handin Quest X' ... flipping on each map change"). `phase("storage")` used to be set BEFORE the call, so every tick where storageTrip() declines — no keeper known, nothing depositable, on cooldown, or (the common one) the keeper is on another map and the bag is NOT blocking — still flipped the label to "storage", fell through to kill/gear/bulk-accept, and flipped back. Measured on ClericFresh: 2930 PHASE lines in 105 minutes, oscillating ~4x/second, with `storage` on one side of EVERY top pair — kill<->storage 1139, bulk-accept<->storage 550, gear<->storage 362, travel<->storage 188. The trigger was the surplus one, logging "8 reclaimable slot(s) ... with 30 free — going now" while 30 slots sat free. Two costs, the second much worse than the noise: (1) the phase tally and the operator's view of what the bot is doing were fiction; (2) phase() CLEARS THE HARD-WEDGE WATCH on every change, so a permanent 4Hz flip meant the wedge detector could never fire, whatever the bot was really stuck on. A phase is a statement about what this tick DID. Deciding not to act is not a phase. Defer to a sub-phase the call set itself: storageTrip() labels "storage:travel-to-keeper" on the travel branch, and a blind post-set here would overwrite the more specific label with "storage".
- **scripts/level_quest.lua:3982** — MOUNT (operator 2026-07-17, capture-derived): at lvl-20+ with NO mount, seek this town's ITEM MERCHANT (Kenton@Eld / Pey@RouN — Role Merchant/Item) and buy the Type1/Class23 mount. discoverServices()'s mount- search keeps probing town NPCs (past the 3 core services) until it opens the item merchant; noteShop's tryBuyMountFromOpenShop() buys the mount on open. Bounded by mountSearchedMap[map] (marked once a town has been fully probed with no mount). A mount = the swarm-transit fix (ride through mob groups) + ~50% travel.
- **scripts/level_quest.lua:4158** — INSTANCE DRIVE (q60006 promotion + reusable): pursue an active SCENARIO quest. If we're NOT inside the instance yet, go to its start NPC and re-drive it — the server fires the SCENARIO warp on the interaction. If we ARE inside (scenarioArea set), the HOOVER below (want=all mobs) clears each room. Data-driven. scenQ + inInstance were computed EARLY (near the restock gate) so town-trip phases are suppressed in the instance; reuse them here. Re-entry only fires when we've genuinely LEFT the instance map (not on a between-room scenarioArea()=="" gap, which used to reset the sweep + spam a NO-ROUTE travel). ⛔ DO NOT TRAVEL TO THE INSTANCE WHILE A PRE-ENTRY TOP-UP IS STILL OWED (2026-08-13). These two goals were fighting over the SAME resource — travel — and neither ever finished. Measured on MageFresh: restock pulls it RouVal01 -> RouN ("HP stones empty, go to town"), then this gate immediately pulls it RouN -> RouVal01 before it ever walks to the healer, ~55s per lap, for HOURS. `restock: buy N HP stones @Julia` never once appears in the log and money never moves off 140 cen; the time budget reads restock=53% instance-enter=32% kill=0% and exp sat frozen at 37633. The top-up is a PRECONDITION of entry, so it must run to completion first; alternating between a precondition and the thing it gates is the ping-pong, not progress.
- **scripts/level_quest.lua:4176** — ⛔ DO NOT WALK INTO THE INSTANCE ON A BROKEN BAR. Measured on MageFresh: it entered at 122/407 HP with 0 stones and died in 3-4 SECONDS, 91 times in 22 minutes (median survival 4s, only 3 stone uses in the whole run because it died faster than it could heal). At the clone's ~30 dmg/s, 4s is exactly the 122 HP it walked in with — so this is not damage, sustain, or money: it is starting a fight already lost. My own "enter under-stocked when broke" escape turned a freeze into that loop, and the operator's rule stands — out-of-money symptoms are downstream of death loops we cause, so the fix belongs here, not in the wallet. Recover first; the heal/restock phases own that and will run because this returns without claiming the tick's work.
- **scripts/level_quest.lua:4239** — PRIORITY (operator 2026-07-03): do the FATTEST-EXP quest first. Quests fall into 4 priority BANDS (best first): [1] repeatable+ok, [2] non-repeatable+ok, [3] repeatable+deprio, [4] non-repeatable+ deprio. Flee-deprioritization stays the STRONGEST gate (a quest that overwhelmed us drops below every not-deprioritized quest until the next level-up); repeatable-preference is the band split; EXP orders WITHIN a band. We FOCUS `want` on the single highest-exp quest in the top non-empty band that has a reachable (non-blacklisted) mob — so the bot commits to the fattest quest (and completes it) instead of grinding whatever mob happens to be nearest. Killing that quest's mob still incidentally credits any other active quest needing the same mob. TODO (P1 remainder, operator 2026-07-03): SKIP a repeatable whose exp < 10% of the level-band median quest exp, or < 0.1% of the level-up XP span (needs band-wide quest exp + the level XP curve) — else a trivially-low repeatable in band 1 outranks a fat non-repeatable in band 2. Ordering-by-exp is in; the low-repeatable skip is the remaining piece. P4: make that floor a level-based curve.
- **scripts/level_quest.lua:4393** — DEMOTE trivially-low repeatables out of the preferred band (operator 2026-07-21 — the bot kept farming Ruined Garden 2 (q233, 100 exp, <1% of a level at lvl24) while 2000+ exp one-time quests waited). bands[2] = safe non-repeatables; their fattest exp is the ceiling. A band-1 repeatable with exp*RATIO < that ceiling drops to band 3, so every safe one-time quest (band 2) is worked FIRST and the repeatable becomes filler done only when they're exhausted. A fat-enough repeatable stays in band 1 (repeatables are infinitely farmable — prefer them when exp is comparable). Uses ONLY exposed quest exp — no level curve. Closes the P1 low-repeatable skip (the documented TODO above at the band-split comment). DYNAMIC / auto-un-demote (operator 2026-07-21): the ceiling is derived from bands[2] = the LIVE WORKABLE set (activeKill already drops done/Tower/UNSOLVABLE quests; the band loop drops blacklisted mobs via #mobs>0), and it is RECOMPUTED every tick. So if the only >10x one-time quest later becomes impossible (questSolvable flips, mobs blacklisted as nav-unreachable, quest completed/handed-in) it leaves bands[2], the ceiling falls, and the repeatable UN-DEMOTES automatically next tick -- never stranding the bot on a repeatable it demoted for a quest it cannot do. (Proof: unsolvable q2560=2146 exp is absent from bands[2], so it never inflates the ceiling.)
- **scripts/level_quest.lua:4411** — ⭐ FINISH WHAT YOU STARTED — don't demote a repeatable that is nearly DONE (2026-08-05). The demote looked ONLY at exp, so Poor Merchant(q2557) at **6/8** (two SilverySlime kills from its payout) was shoved to band3 by `150 * 10 < 1935`, and never competed again: BAND is chosen before any within-band sort, so the finish-what-you-started comparator below could not even see it. Meanwhile focus sat on Rare Material 4(q2511) at prog 0/0 purely on exp. Proven by the FOCUS line: `band=2 local=false prog=0/0 (0%) exp=1935`. This is exactly the operator's rule (CLAUDE.md 🎯 QUEST COMPOSITION): "demote only TRIVIAL repeatables; a DECENT repeatable should be preferred/done". A half-finished one is by definition not trivial — the remaining cost is small and the reward is immediate.
- **scripts/level_quest.lua:4567** — KITE FIRST. This branch had NO kite at all, and the cost was measured: MageFresh stood at exactly 113u for three consecutive shadow-clone fights while its HP fell 330 → 2, never once retreating. The clone does not regenerate (measured: HP falls monotonically all fight), so backing off to heal banks the damage permanently — the bot does not need to survive one long fight, only to accumulate enough damage across as many passes as it likes.
- **scripts/level_quest.lua:4590** — CLOSE THE DISTANCE OURSELVES — there is nothing else that will (see the header). This used to be a "SERVER-FOLLOW SAFETY NET" that only stepped in after waiting 900ms (past CHASE) or 2500ms (past engage range) for a follow to happen. Those delays were paying for a mechanic that does not exist, so they were pure dead time in the middle of a fight; the bot stood still, took hits, and failed casts with 0x0FCA out-of-range while it waited. ONE gate survives, and it is real: WALKING CANCELS BASHSTART. bot.damageDealt(2500) — a connecting swing in the last 2.5s — proves we are already in effective range and the swing stream is running, so walking then would trade real DPS for nothing. Close only when the hits have actually stopped. The short throttle is anti-thrash, not a wait.
- **scripts/level_quest.lua:4629** — Clone FLED (operator 2026-07-08): at ~80% HP the shadow-clone teleport-jumps far (~2400u) and then DESPAWNS — it is NOT meant to be killed. Chasing is doubly futile: (1) it despawns on its own, (2) while it flees it STUNS us (abstate) so walkTo FAILS anyway until it's gone. So DON'T chase and DON'T spam blocked move commands — break off the auto-attack and just wait. Next tick, once it despawns, #ag drops to 0 → we fall through to the ROOMBA sweep (the intended flow).
- **scripts/level_quest.lua:4913** — 🩸 IN THE RISKY BANDS ONLY (b >= 3 = everything deprioritized/risky), rank by WHAT HAS ACTUALLY KILLED US before anything else. Live loop this fixes (2026-08-05): q273 killed the bot 3x in 10 minutes, was correctly deprioritized each time — and was then re-picked anyway, because when EVERY quest is deprioritized the "least-bad" fallback simply re-chose the killer. Deprioritizing only helps while something un-deprioritized remains; once the board is flat it is a no-op. Map ping-pong RouVal02->ValDn01->RouN x3 with exp NET -476/10min was the visible symptom. This is ORDERING, never a veto (operator 2026-07-16: an absolute cap "abandoned winnable fights", and a bar that can reject 100% of options is worse than no signal) — the board cannot empty, the bot always still has a quest, it just stops re-picking the one that keeps killing it. Deliberately confined to b >= 3: in the healthy bands the existing locality/progress order works and must not be disturbed by a signal meant for the last-resort case.
- **scripts/level_quest.lua:4925** — ⚠️ BOTH ranking terms here must OUTLIVE A RESPAWN. The first version of this sort ranked on a Lua-LOCAL death table; locals reset on every script re-apply and this bot respawns constantly (deploys, deaths, handoffs), so measured right after shipping it the bot went straight back to mob4002 @ValDn01 (53% of a 6-minute budget) — the count meant to rank it last had been wiped. Both terms are now persisted to the knowledge store (PVC): the deprio marker, and the death count via bot.questDeaths(). Same lesson as the mob threat table: knowledge that resets every session cannot influence a bot that restarts every few minutes.
- **scripts/level_quest.lua:5066** — ⚠️ The old text here said "fattest reachable quest", which is STALE and actively misleading: the sort is localMob FIRST, then progress-to-completion, then exp. Reading that string cost a whole /incremental pass chasing a "picks fattest exp" bug that doesn't exist (2026-08-05). Print the ACTUAL decision inputs so the winner is explainable — band, locality, kill progress and exp — per the decode→log-it rule. ⚠️ LOG THE FOCUS DECISION ON *CHANGE*, NOT VIA dbg() (2026-08-05). dbg() has a SINGLE GLOBAL 2s throttle shared by every call site (`if bot.now() - lastDbg < 2000 then return end`), so all callers compete for one slot and whichever fires first wins — the focus decision was usually DISCARDED, not missing. Measured: 0 focus lines in 63s of readable tail while `restock:`/`grind mob` lines showed. A decision must never be dropped because an unrelated status line won a shared timer. On-change logging is the right shape here: it is exactly as frequent as the thing it reports, so it needs no throttle at all (contrast the [leash] line, where I wrongly assumed "only on a new max" was throttling — 40 mobs meant 40 independent maxima. Ask how many entities can emit a line before adding it.)
- **scripts/level_quest.lua:5211** — ⛔ ABSENT MEANS *NOT IN VIEW* — ASK, DON'T INFER (operator 2026-08-11: "I'm seeing 'Mandragora no field spawn' which is simply false"). This timer used to accumulate purely because grindStep() returned false, and then reported that as "NO FIELD SPAWN". It never once looked at what was actually in front of the bot. Measured at the moment of the operator's report, JcqMage: STUCK-DIAG grindStep=false ... wantedInView=18 ignored=0 nearestDist=249 18 Mandragora in view, the nearest 249u away — and the bot declared the mob doesn't spawn here and BLACKLISTED it, dropping a perfectly good quest. Same shape as the Cleric's "Imp NO FIELD SPAWN" while standing in a field of Imps. A failure to ENGAGE is not evidence about SPAWNING.
- **scripts/level_quest.lua:5284** — walkTo MOVEFAILs), the pull can't isolate → ABORT to fight-in-place (deal damage instead of passively tanking the crowd). operator 2026-07-22 (live death: crowd 7, move=false, hp 72→15% while dragging + dealing ZERO damage).
- **scripts/level_quest.lua:5334** — ⛔ NEAREST WINS — a WANTED aggressor does NOT outrank a much closer one (operator 2026-08-13). This used to `return bestW or bestA`, i.e. prefer a wanted-type attacker at ANY distance. Measured on ClericFresh: Skeletons were biting it at 20-48u while a Punch Mage Book (a quest type) attacked from 811u, so bestW picked the FAR one and the bot walked ~600u toward it dragging the pack — aggro 1->3->4->5, hp 585->194 (33%), a self-heal and a soul stone burned, and no kill. Operator: "has a rat tail, then turns around and walks to the target mob instead of ever dealing with the mobs following it, making life much harder for itself." Everything already hitting us has to die regardless of quest credit, and the closest one dies soonest for the least damage taken. So take the nearest, and let `want` decide only when the two are comparably close (within 1.25x distance => 1.5625x in the squared space we compare in).
- **scripts/level_quest.lua:5492** — HEAL TO FULL before starting the fight (operator 2026-07-04 P0): a wanted mob is in view → top the HP reserve up to ~full BEFORE committing, so we never engage at 40% (instant disadvantage). Fires ONLY here (right before an engage), never while idle → a poison/DoT can't drain the reserve between fights.
- **scripts/level_quest.lua:5514** — Label is "kill", NOT "grind": in the crutch-log vocabulary GRINDING means "killing WITHOUT quest credit" = a failure state, so calling the ordinary quest-mob kill loop "grind" made the time budget read like the bot was wasting a third of its life. Measured over 24 min: 16 KILLED / 16 QUEST_MOB_KILL credits (1:1, split 8x Insomnia 2 + 8x The Marlone Clan Megaton), 0 LAST-RESORT lines — this phase is entirely productive questing.
- **scripts/level_quest.lua:5567** — DRAG-STUCK detector: if the drag can't actually MOVE us (surrounded / attack-locked → walkTo MOVEFAILs), track position progress and abort after PULL_DRAG_STUCK_MS of no >60u advance. Otherwise we loop here forever re-issuing a failing walkTo while dealing ZERO damage and tanking the whole crowd → death (live 2026-07-22: crowd 7, move=false, hp 72→15%). Aborting → fall through to the fight code below, which chases/bashes the target + casts (deal damage). You can't isolate a mob in a dense swarm; fighting is the right fallback. NOT a bandaid — the real fix (survive the swarm) is gear/party (P1), tracked.
- **scripts/level_quest.lua:5649** — the zigzag leg is done and THIS is the direct approach from the new angle — re-arm the next zigzag and give this approach a fresh stall window measured from a fresh baseline
- **scripts/level_quest.lua:5874** — HP or SP stone reserve is empty or below 10% of capacity (so we restock early, not at 0). Counts are seeded at zone-enter (NC_CHAR_BASE CurHPStone@38/CurSPStone@40); -1 = unknown → fall back to the depletion flags. SP was folded in 2026-07-01 (operator P1 split: RESTOCK SP stones on the same town trip, not just USE the reserve) — the trip itself is still gated by the `broke` budget check in tick(), so a poor bot doesn't town-loop for stones it can't buy.
- **scripts/level_quest.lua:5997** — AUTO-EQUIP better gear from inventory (quest rewards / drops). Right class (usableByUs → the host's UseClass ladder for OUR class), level-appropriate (demandLv<=level), and BETTER than what's worn: slot empty, OR higher demandLv (higher tier), OR same level + higher grade (rarity). Returns true if it equipped. ⛔ THIS WAS `FIGHTER_EQUIP = {0,2..7}` — the GEAR half of the hardcoded-class bug (operator 2026-08-11: "All other than fighter broken: Buying fighter skills, trying to equip fighter gear"). The SKILL half was fixed on 2026-08-06 with usableByUs(); the gear half was left baked, so on a Cleric/Mage/Archer the bot STILL bought and tried to wear Fighter armor/weapons it can never equip, AND skipped its own class's gear in the very same shop — and worse, `learnSmithTier` measured every smith's tier in FIGHTER armor, so the "can this smith upgrade me?" routing was answered for the wrong class too.
- **scripts/level_quest.lua:6118** — RESOLVE the previous fire FIRST — BEFORE the shop-open guard. A 0x038F ("item refused") reject also resets ShopOpen in ZoneView (that reset is really meant for 0x0383 out-of-range; on 0x038F the shop is still open server-side), so checking only after the guard would bail on the false "closed" and never record the refusal → reopen → re-fire the same slot forever (the live 0x038F town-loop). A real sale EMPTIES the cell (verified QuestsNew.pcapng: OK = CELLCHANGE slot->0xFFFF + CENCHANGE + 0x0381), so "slot still holds the same item ≥900ms after firing" = the server refused it → skip it this trip + log.
- **scripts/level_quest.lua:6151** — PACE sells to ONE stack per ~900ms (operator P1 2026-06-26: the SELL_ACK 0x0382 rejects — 13 rejects vs 2 OKs in one window — are almost certainly the SAME rapid-fire root cause already proven + fixed for buys, "the server accepts only ONE [req] at a time" — never applied to sell, which fired the WHOLE bag's sells in one tight loop with zero pacing). Mirrors buyGear's fix.
- **scripts/level_quest.lua:6156** — ⏱️ TRANSACT stage. NOTE the 900ms floor below: sells are paced ONE ITEM PER 900ms because a refusal is detected by "the slot still holds the same item ~900ms after firing" (there is no positive reject packet we trust). With a bag of ~40 sellable stacks that is ~36s of pure waiting per trip, which is a prime suspect for sell being 47% of all bot time — but MEASURE it here before changing the pacing.
- **scripts/level_quest.lua:6550** — ✅ CRUTCH REMOVED 2026-08-06. It bounded the trip to 3 deposit-less attempts because an unfinishable trip was eating 91-96% of the time budget. Every cause is now fixed and proven live: • deposits confirm on the SOURCE BAG CELL clearing (not any global cell change); • StorageBox no longer flip-flops 6->0 (adopted only when all items agree); • the free-slot picker no longer re-targets a cell we just filled; • items the server permanently refuses are remembered and never re-offered; • and crucially TRAVEL only fires when the bag is genuinely BLOCKING — that gate, not the deposit failures, is what let a broken trip monopolise the bot. So the failure mode the cap guarded against is now structurally prevented regardless of whether a deposit succeeds. Proven: 6x "DEPOSIT ok (bag cell cleared)", bag 48->46, then "nothing left to deposit — done".
- **scripts/level_quest.lua:6630** — ⭐ THE REST OF THE WAY TO ~15 (operator 2026-08-06: "39/48 from a storage trip is only a half pass — should drop to around 15 when done properly"). Deduping duplicate stacks got 41 -> 32; the remainder is a long tail of SINGLE stacks, and two kinds of them are dead weight no matter what we later decide to drink: • DRIBS — a handful of a consumable that stacks to 50: 1 Speed Increase, 2 Antidote, 2 Aim, 1 Concentration, 2 Strength, 2 Divine Magic, 2 Magical Defense. A quantity that small cannot change an outcome; it is occupying a slot to no purpose. Measured against the item's OWN maxLot (client data), not a baked list of ids — a maxLot-1 item is never a drib. • CURIOS — maxLot-1, non-equippable oddments the driver has no code path for at all: Chipped Mystery Vault, Old Bag (15 Days), EXP Boost Card, Hobby. ⛔ Quest-objective items are excluded via questItemKeep(), and equipment (type 0) is untouched — those keep their existing rules. Deliberately NOT keyed on "is this a heal?": drinkHpPotion() is still LEARNING which items heal, and an unlearned item is unknown, not useless — dumping a full stack of HP potions because we had not tried one yet is exactly the "nothing yet read as an answer" trap.
- **scripts/level_quest.lua:6761** — ⛔ CROSS-MAP TRAVEL ONLY FOR A GENUINELY BLOCKING BAG. The caller fires this whenever fewer than 6 slots are free — fine as an OPPORTUNISTIC top-up while we are already standing in the keeper's town, but ruinous as a trigger for a multi-hop expedition. Measured 2026-08-06: "bag full (5 FREE)" sent the bot RouVal02 -> Eld over and over, 82% of a 12-minute budget in storage:travel-to-keeper at +0 exp. (That was my own regression: the original gate was cheap because it also required isTownMap(), which I removed to enable travel — turning an errand into an expedition.) Cost differs by orders of magnitude between "walk across town" and "cross three maps", so the threshold must too. ⛔ REVERTED 2026-08-06, SAME DAY, BY MEASUREMENT. I added `or surplus >= SURPLUS_TRIP_MIN` here on the argument that a trip reclaiming 8+ slots "pays for the journey", and that it could not thrash because depositing clears the surplus. Both halves were wrong in practice: measured 77% of a 14-minute budget in storage (45%) + storage:travel-to-keeper (32%) at +0 exp — which is the SAME 82%-budget regression this guard was written to close, re-opened by me from the other side. The asymmetry the first fix identified still holds and is the whole point: DEPOSITING is cheap, but TRAVELLING to the keeper costs orders of magnitude more, so only a genuinely BLOCKING bag justifies the journey. Tidying surplus is worth doing when we are already in the keeper's town — which happens constantly anyway, since hand-in NPCs live there — and is never worth a multi-hop expedition. The surplus trigger stays on the START gate (cheap when local); it just must not summon travel.
- **scripts/level_quest.lua:6787** — ⛔ NO isTownMap() CHECK. `townMaps` is SESSION state, filled in only when a shop gets classified here, so on a fresh session it says "not a town" for a town we have known about for days. That made the trip arrive in Eld and immediately return false, handing the tick to the quest-mob seek which walked straight back to ValDn01 — a 12-second Eld<->ValDn01 ping-pong that burned 62% of a 10-minute budget at +0 exp. Knowing a storage keeper is on THIS map (from the persisted store) is the qualification; isTownMap is a proxy that adds nothing here and is false-negative exactly when the session is young.
- **scripts/level_quest.lua:6804** — The zone NPC seed is AoI-limited, so arriving in Eld far from the keeper leaves npcLocation nil and this backed off for 2 minutes — the bot travelled to the right town over and over and then stood there unable to find him (91% of a 12-minute budget in travel-to-keeper, 0 deposits). npcCoord() is the CLIENT's own canonical NPC position (MobCoordinate.shn), which is exactly what the real client uses to place the map marker — available whether or not he is in view.
- **scripts/level_quest.lua:7206** — TRAVEL-STALL guard: bail only if travel has made NO PROGRESS for ~45s — measured by real movement, not a fixed map-change clock. A valid cross-map walk to a far gate keeps the SAME map for ~60s while the position steadily advances (EldGbl02→gate ≈ 4200u): that is NOT a stall, and the old map-only timer wrongly aborted it mid-walk. So reset the timer whenever the map changes OR the position has advanced meaningfully; only a genuinely frozen bot (no map change AND no movement) for 45s is stuck. Same progress-based doctrine as the walk-stall guard.
- **scripts/level_quest.lua:7544** — no candidate's giver is on THIS map: from a field, head to TOWN (the giver hub) if anything is still acceptable at all; from town, the plan is done ⛔ THIS LINE WAS THE PING-PONG (measured 2026-08-18). It read "I am not in RouN and I still want to accept things, so go to RouN" -- with no idea WHY the bot was standing where it was. A hand-in had deliberately travelled to Eld to turn in `Lost Love 2 (q430)` at npc116; this fired on arrival and dragged it straight back, six times, until the quest was abandoned and its 1173 exp lost. Two fixes: go to where the givers ACTUALLY are (computed), and never walk out on a hand-in that this very map can complete.
- **scripts/graph/stay_alive.lua:1** — stay_alive (state) — survive ONLY: heal-first / stone / flee-while-healing. Does nothing else, so it's safe to run 24/7 and to flip to anytime for "90% control" while the operator does manual things. survive()/overwhelmed() come from the shared survive.lua helper.
- **scripts/level_quest.lua:2424 — "storage trigger"** — ⛔ THE TRIGGER ASKED FOR SOMETHING THE ACTION REFUSES (fixed 2026-08-18). `storageTrip()` only crosses a map when the bag genuinely blocks (`bagFull or free < 1`, see the 6761 entry — that guard was itself re-opened and re-reverted the same day on 2026-08-06). The START gate fired on two weaker reasons as well, `bagFree < 6` and `bagSurplus >= SURPLUS_TRIP_MIN`, without checking that the keeper was reachable without travel. With the keeper off-map that is a gate whose only cure is the action it is blocking: measured over a 2h buffer, **ZERO deposits, 1577 bare `storage` phase labels, and `storage:travel-to-keeper` never reached once**, while the surplus line announced "8 reclaimable slot(s) with 30 free — going now" every 30s. The 6761 entry had already concluded "the surplus trigger stays on the START gate (cheap when local); it just must not summon travel" — the locality half was never implemented, which is what made it noise instead of a fix. Now the weak reasons require `npcMap(storageNpc) == bot.map()`; crossing a map still requires a blocking bag. Trigger ⊆ what the action will do.
