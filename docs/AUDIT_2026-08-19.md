# Code audit — 2026-08-19

Four parallel audits (C# manager, C# packet decoder, C# host+UI, Lua driver) against this
project's own hard rules. Findings are recorded here so none are lost; fixed ones are struck
through with the commit.

Rules used as the criteria (from CLAUDE.md):
1. NO HARDCODED game facts (npc/mob/item/skill ids, maps, coords, prices)
2. ZERO IS A VALID VALUE — never a sentinel
3. Wiring an action means wiring its RESULT packet
4. A `-1`/"unknown" fallback is a DECODE GAP, not an acceptable state

Bug shapes searched for, all proven real in this codebase today:
A comment/log contradicts the code · B stale state read as an answer · C guard cleared by the
action it gates · D lifetime (work outliving its session) · E event emitted before the state it
describes · F decision treated as an outcome

---
## POSTSCRIPT 2026-08-20 — what the audit got right, and what it missed

The morning after this audit the live bot was found **hard-wedged for 9.4 hours**: frozen at
`(5899,6530)` in RouN, `sell:travel-to-town` 97% of 34,475s, +0 exp, 0 deaths, 45 CRITICAL lines,
and `MAP-CHANGE LOOP: Eld -> RouN (5x in 60s)` repeating for the whole window.

**The audit predicted a wedge of exactly this shape** (HIGH #1: hardcoded `SELL_NPC = 29` used raw,
so a bag-full trip cross-maps to npc29’s town and, if selling never succeeds, the bag stays full and
per `fiesta-bagfull-blocks-everything` the whole quest loop halts). That prediction was close enough
to be dangerous: **it was not the cause.** Reaching for it would have shipped a real improvement and
left the bot wedged.

The wire said something else. Two lines from the same instant:

```
07:25:14.678 CRUTCH[CRIT] HARD-WEDGE: pos @(5899,6530) ... phase=sell:travel-to-town map=Eld
07:25:08.102 ⛔ WATCHDOG: MOTIONLESS for 45s at (5899,6530) on RouN
```

The Lua thought it was on Eld; the host knew it was on RouN. `mapOf()` preferred `pulse.map`, and
`pulse` is refreshed only by `questSnapshot()`, called from the quest-scoring loop **late** in
`tick()`. The sell branch returns before that loop — so the bag filled on Eld, the snapshot froze
with `map = "Eld"`, `sellLoot()` read the frozen value while standing in RouN, travelled to RouN,
returned early again, and the only code that could have corrected the stale map sat behind the branch
the stale map kept taking.

That is bug shape **C** from the list above (a guard cleared only by the action it gates), and it is
the same shape as `fiesta-gate-depends-on-what-it-gates`. The audit hunted shape C in the C# and
found it in `ObserveGates`; it did not look for it across the Lua caching layer, which is where the
expensive instance was. **Caches are guards**: "is this value fresh" is gated by the code path that
refreshes it.

Fixed in the lua repo (`freshPulse()` — a cached answer is only an answer for the tick it was taken
on) and in `ConfirmHopArrival` on the C# side. Verified live: position moved within one minute of
applying, exp 173,900 -> 174,038.

Two process notes worth keeping:
- **An audit finding that matches the symptom is not a diagnosis.** HIGH #1 described this exact
  failure mode in advance and was still the wrong answer; only the interleaved log settled it.
- The `HARD-WEDGE` line blames "client<->server position DESYNC". That is the detector’s **guess**,
  printed as though it were a reading. It sent the first pass of this investigation the wrong way.
  A diagnostic should report what it measured (position and exp frozen) and not name a cause it has
  not established.
---
## FIXED THIS PASS
- [x] **Phase accounting was QUADRATIC** (`BotHandle.NotePhase`) — added `now - phaseStart` every
  tick instead of the delta, resetting the start only on phase CHANGE. A phase of duration T with
  N ticks accumulated ~N*T/2. **Every TIME-BUDGET percentage reported today was distorted**, worst
  for the longest phase — i.e. the one under investigation. `2237947`
- [x] **Belief advanced before the send was legal** (`WalkAsync`) — `BeginMove` ran before
  `ActAsync` checked Phase/ZoneSession, so a walk issued while not in zone still moved the believed
  position. Same family as the 108u desync proven on the wire. `2237947`
- [x] **FollowAsync outlived its connection** — captured `session`, CTS linked to the BOT lifetime;
  kept writing MOVERUN into a disposed link after a reconnect. `2237947`

---
## HIGH — not yet fixed
(see the sections below; each entry carries file:line, the rule/shape, and why it matters)

### Explains an operator-reported bug
- **NPC/gate seed missing on arrival** (`EventStreamEndpoint.cs:117-126,235-243`) — shape E.
  `MapChanged` posts `t:"map"` AND sets `resendState`; the pump runs the resend BEFORE draining
  `oob`, so the wire order is `state` then `map` — and the client's `map` handler clears
  `mobs`/`npcs`, wiping what the resend just installed. `FullState`→`EntityPanel` is the ONLY path
  that sends the whole-map NPC seed (the per-entity path is AoI-only), so after every map change
  the gate/NPC layer stays empty until the socket reconnects. Operator: *"Bot just arrived in
  Burning Hill WITHOUT NPC (gate) SEED! npcs are only filling in as we get near."*

### Mine, introduced this session
- **0x244E target fallback is fiction** (`ZoneView.cs:2198`) — PDB `SKILLBASH_HIT_OBJ_START_CMD`
  is SizeOf=6 with `targetobj@2` ALWAYS present. My comment invents a "shorter form" and falls back
  to handle 0, which is a real handle. Rule 2 + shape A.
- **`SetAnnounce` persists to the roster but not `handle.Options`** (`BotManager.cs:104`) — `Relog`
  re-spawns from `Options`, so the setting survives a process restart but is reverted by the far
  more common relog, while the toast claims "persists across reconnects". `SetPacketLog` same hole.

### HIGH — correctness
- `ZoneView.cs:2805` login bag snapshot drops item id 0 (real item "Leather Boots") → BagFreeSlots
  over-reports → my AutoLoot gate is wrong until a CELLCHANGE touches the slot. Rule 2.
- `ZoneView.cs:2845` EQUIPCHANGE treats equipping item 0 as UNequipping. Rule 2.
- `ZoneView.cs:1246-1252,2905` login skill list reads only `skillid@0` and ignores `cooltime@2`
  (PDB `PROTO_SKILLREADBLOCKCLIENT` SizeOf=12) → after a relog every skill reports READY and the
  rotation eats 0x0FC8 until each has been cast once. Rule 4.
- `ZoneView.cs:1920` SOMEONESWING_DAMAGE passes Damage=0 (the packet has no damage field, but the
  previous `_entityHp` is right there) → every such hit is logged "WHIFF (no connect)" and skipped
  for threat/metrics.
- `ZoneView.cs:2699` `_entityHp`/`_entityMaxHp` are never cleared on map change though every other
  per-handle table is → handles are per-map and reused, so HP reads back from the previous map.
- `lua:3782` **`outmatched()` is overridden by a mob COUNT**: `shedStep()` returns false on
  `n < SHED_MIN_PACK` (3), so "dead in 2.3s" against TWO mobs neither escapes nor logs — the
  OUTMATCHED line is inside the `if`. Kills the bot silently.
- `lua:388` `SELL_NPC = 29` hardcoded and used RAW (unlike HEALER, which is now a fallback seed) →
  every sell trip cross-maps to npc29's town; on a map without it, selling never happens and a full
  bag halts the whole quest loop.
- `lua:337` `XPFIELD = "RouCos02"` / `AVOID_MAP` hardcoded map names; xpGrind travels there at any
  level. Rule 1.
- `lua:821` `hardWatchAt = nil` inside `phase()` writes a stray GLOBAL (the local is declared ~1300
  lines below) → the wedge watcher's reset-on-phase-change never happens → false HARD-WEDGE reports.
- `lua:1369` `engagedAt or bot.now()` — `engagedAt` is 0 and 0 is TRUTHY in Lua, so the fallback
  never fires, `dur` is ~epoch, and the "only in a sustained fight" gate is permanently satisfied.
- `BotManager.cs:453+` `ActionResult.Sent` returned on SEVEN refusal paths (cooldown, out of range,
  windup, full HP); `bot.cast()` maps Sent→true, so the rotation is told a cast happened when
  nothing hit the wire.
- `BotManager.cs:1365,1426` travel stamps `SetCurrentMap(expected, null, ...)` — overwrites the
  server-resolved name with intent AND leaves `CurrentMapId` on the previous map, recreating the
  name/id disagreement behind the nine-hour wedge while bypassing the mismatch guard.
- `BotManager.cs:1198` `ObserveGates` returns early unless 2500ms since the map change, but both
  post-hop calls run within a few hundred ms → gate auto-discovery never runs. Shape C.
- `BotManager.cs:154` `StopAsync` reads Phase/ZoneSession without `HandoffInFlight` → the clean
  logout is sent into a dying link, or not at all, while reporting a clean stop.
- `BotManager.cs:1252` unknown position becomes `(0,0)` for route costing → Dijkstra measures from
  the map origin and picks the wrong gate. Rule 2.
- `BotManager.cs:576` `MountItemClass = 23` hardcoded (and duplicated in Lua, so they can drift) →
  if wrong, dismount fails and EVERY gate hop silently fails.
- `BotEndpoints.cs:1346` Iyzel mob-id range 8100-8138 + 9186/9187 baked into the quest board.
- `BotApi.cs:562` skill-match returns 0 for "no match" — skill id 0 is real.
- `BotEndpoints.cs:1287` + `watch.html:296` `SustainableHealDps` 0 → reported null → the "cannot be
  out-healed" banner can never fire in the case it exists for.

### MEDIUM / LOW
Full lists (37 Lua findings, 34 host/UI, 30 decoder, 27 manager) are in the session transcript.
Recurring themes worth naming:
- **37 `bot.X and bot.X()` guards in the Lua that do not guard** — `bot` is userdata and MoonSharp
  THROWS on a missing member. `botHas()` exists for this and is documented three times in the file.
- **Zero-as-sentinel is systemic**, not incidental: mob 0, item 0, skill 0, npc 0, quest 0 all have
  "== 0 means absent" tests somewhere, in a codebase whose top golden rule is the opposite.
- **Logs that lie**: the lethality "DEMOTED" line (nothing is demoted), RETALIATE's "WITHOUT
  walking", `hitBack` (fixed today), "re-click 1/8" against a 1-iteration loop.
- **Dead machinery**: `fleeing` is never set true (~22 lines unreachable), `depriRetry`/
  `DEPRI_RETRY_MS` write-only so the documented deprioritization expiry does not exist,
  `MeleeRangeExperimentU` can never engage, `instEnteredAt` never read.
